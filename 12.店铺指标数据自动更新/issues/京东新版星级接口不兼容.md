# 京东新版星级接口不兼容

## 意图

让京东店铺星级采集使用当前京麦页面真实返回的数据，并把风控返回与代码兼容性问题分开判断。

## 背景

- 2026-09-10 真实运行时，京东1店、3店均已自动登录成功，说明本轮失败不是登录按钮定位问题。
- 两店页面均进入 `https://shop.jd.com/jdm/shopstar/vane/VaneContainer`，页面文字显示“当前星级：暂无”“数据更新时间：Invalid Date”。
- 京东1店证据显示店铺星级读取等待 60000 毫秒后超时；京东3店同时出现关闭遮挡弹窗失败。

## 真实采集证据

- 真实浏览器资源中没有请求旧接口：
  `dsm.shop.vane.view.core.export.ohs.stars.service.VaneBasicFacade.queryVaneBasic`。
- 当前页面实际请求并成功返回新接口：
  `dsm.shop.vane.view.core.export.ohs.stars.service.VaneStarsFacade`。
- 该接口 HTTP `200`、业务 `code=200`，返回字段包含 `scoreRankRateGrade=5.0`、`scoreRankRate=98.01`、`validOrderNum=1434`、`customServiceConsultScore=10.0`、`logisticsLvyueScore=9.9` 等真实数据。
- `VanePrejudgmentFacade.queryVanePrejudgment`、`VaneDegradationFacade.queryDegradationInfo` 等辅助接口本次确实返回 `code=601`，存在京东风控/限流，但核心 `VaneStarsFacade` 并未被拦截。

## 当前结论

当前星级失败有两个叠加原因：`dcf34e3` 把 Git 旧版可用的星级入口改成了新版地址，同时采集器仍按旧接口协议等待“基础指标 + 星级汇总”两份响应；新版页面只请求并返回 `VaneStarsFacade`，程序因此等到 60 秒超时。风控是并存问题，不是本次核心星级数据完全读不到的唯一原因。

## 已实现

- 新增独立协议层，识别旧版“双接口”和新版 `VaneStarsFacade` 单接口。
- 恢复 Git 旧版使用且今天仍可用的星级入口 `jdsz.jd.com/szweb/view/service/shop-experience-score.html`。
- 新版字段直接映射为统一指标：`scoreRankRateGrade`→店铺星级，`scoreRankRate`→店铺星级排名，另写入有效订单、客服、物流、售后和商品体验得分。
- 只有两个主接口的 601 才触发可见浏览器接管；降级和预判等辅助接口 601 不再阻断主流程。
- 新版接口没有返回的复合体验分、旧版明细不猜值，继续按无数据记 0。

## 真实验收

- [x] 京东1店主星级真实采集：5 星、排名 88.32%、有效订单 1794。
- [x] 京东1店完整三页面流程：实际写入 34 条；恢复旧入口复测替换 34 条。
- [x] 京东3店主星级真实采集：5 星、排名 98.01%、有效订单 1434。
- [x] 京东3店完整三页面流程（默认恢复入口）：实际写入 32 条，替换 30 条，2 项未读取。
- [x] 工作簿复核：两店核心星级指标均为 2026-09-10 数据。
- [x] 代码全量测试：123/123 通过。

## 验收清单

- [x] 用真实登录态进入京东店铺星级页面。
- [x] 抓到真实接口响应及业务状态码。
- [x] 证明新版页面未请求旧 `VaneBasicFacade`。
- [x] 确认核心响应包含星级 `5.0`。
- [x] 设计并实现新版响应到统一指标的映射。
- [x] 在京东1店、3店各完成一次真实写入验收。

## 边界

- 不绕过验证码、滑块或京东风控。
- 不把 `601` 风控响应伪装成成功数据。
- 新版未提供的字段不做推算，避免把排名率、分数和明细指标混写。
