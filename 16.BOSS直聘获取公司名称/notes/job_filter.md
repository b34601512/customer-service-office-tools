## 意图
允许按招聘岗位筛选，避免误以为只能找电商运营。
## 背景
原搜索关键词可改但不直观，站点搜索也可能返回标题不匹配的岗位。
## 验收
- [x] 配置明确显示搜索词与岗位包含词，包含词为空则不限
- [x] 多个包含词以逗号分隔，任一匹配保留
- [x] 过滤在企业详情请求前完成；CLI/TUI共用业务规则
## 边界
不固定电商岗位；保持原岗位信息与去重。

## 验证记录
实测搜索客服，包含词电商,抖音：2页30条候选，导出5条，逐条校验标题、全称和唯一ID通过。
自动化49通过、3跳过；本轮验证未导致业务代码返工。
