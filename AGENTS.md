# [AGENTS.md](http://AGENTS.md)

1.本仓库的问题与笔记（issue）在这里：<https://github.com/b34601512/customer-service-office-tools/issues>

2.本地永远是最新，GitHub仓库是用来备份代码的，所有改动测试通过后帮我自动备份到GitHub仓库，并合并到主线，关闭已经完成的issues。
   - **提交只点名加自己改的文件，禁止 `git add -A`**：本仓库常有平行会话/多窗口在同时改代码，`git add -A` 会把别人正在改的删除/改动一起吞掉（2026-09-18 实例：14号整目录被别人清空，被我一句 `git add -A` 提交进去，事后 `2624c80` 恢复）。

3.定期自动更新本agents.md，特别是砍掉冲突的，只保留最新思想。

## 汇报规则（用户 2026-09-17）

- **只在需要用户处理/拍板时才汇报**；日常批次跑完、且没有需要他动的事，就保持安静（不贴数字、不列清单）。
- 必须汇报的情形：需要人工验证（滑块/验证码/短信/扫码）、需要业务口径拍板、碰到业务红线、
  失败且需要他决定是否补跑、结果影响业务判断（少店/金额异常/超期发票等）。
- 沟通格式（同全局 AGENTS.md）：30 字内说核心，单条尽量 ≤100 字，**一次只问一个问题**。

## 业务红线（不可自动操作，用户 2026-09-17 明确）

1. **天猫发票「待同意」单：绝对不许自动点「同意」**（不批准开票申请）。
   原因：客服是**先手工登记到自己表格**、之后才去点同意；自动化抢点同意会让客服误以为“已经有人登记过了” → **反而漏处理**。
   - 允许：只读巡检/同步（读列表、读状态、统计）。
   - 需要“同意/批准/提交”类动作时：停下来问用户。
2. 任何会真实提交/发送的动作（发票回传、企微发送）一律先问用户。

## 失败处置总方针（用户 2026-09-17 拍板，优先级高于下面的铁律）

> 采集问题 → 记录 → 总结规律 → 针对规律找**从根源上解决的干净方案**。
> 落点：每次自动化失败都写进仓库根目录的 [`失败台账.md`](./失败台账.md)（只追加）。

1. **失败后不再自动尝试**：程序里不允许存在"重复请求平台数据"的自动重试 —— 开关、参数、死代码一律不留（半拆=没拆）。
   - 唯一例外：**修复工具自身**的动作（CDP 连接重试、调试端口等待、浏览器会话重启、移动缓存目录）。它们不重复请求平台数据，仍保留；若证明无用也照样砍。
2. **先分析，再决定**：失败后由模型/人亲自定位原因，**不许猜**（"可能是抖动/限流"这种话一次现象里不许说）。判断要不要**手工补跑**，并在台账写明理由。
3. **只出现 1 次 = 观察项**（禁止下结论）；**同一现象 ≥2 次才算规律**；成了规律才允许提程序级优化，且要写清"根源上怎么干净解决"。
   - 同一轮的连环补跑算 **1 次事件**，不算多次证据。
4. **失败要留现场**：报错必须写清——哪家店 / 缺哪张表 / 卡在哪一步 / 等了多久 / 当前页面地址 / 原始报错原文。
5. 台账攒到规律后，改动要做成**根因修复 + 反向断言测试**（删掉的东西用测试锁死"不许再加回来"）。

## 工作约定（用户要求，2026-09-13）

0. **收工/换班时留断点记录**：在**当前项目目录**下写 `断点记录.md`（如 `21.AI自动排班经验/断点记录.md`；覆盖更新，只留最新一份），内容包括：做到哪、下一步、待用户拍板的事、常用命令。**下次开工先读它**。
1. **每次开始新任务，先拉起一个 loop 托管它**（`LoopCreate`，一般用 `triggerType=idle` + `trigger=idle`，配 `maxFires` 上限与 `expiresIn`），防止会话中断导致任务半途而废。
   - loop 的 prompt 里写清：目标、每轮做什么、自检/验收命令、提交推送方式、**哪些事必须停下来问用户**。
   - 每轮唤醒结束用 `LoopUpdate` 写 state/metrics（`continue`）；待办清空或只剩用户拍板项时 `status=completed`；只有明确取消或满足停止条件才 `LoopDelete`。
   - 一个任务一个 loop，别让多个 loop 抢同一件事。
2. 干完的活**当场提交并 push**，别攒着（本仓库 `.gitignore` 只备份源代码：`*.xlsx`、图片、含密钥的配置不入库）。
3. 需要用户/人事拍板的事（业务口径、法定节假日天数、是否加人）**不要自己猜**：停下来问，并在 loop 里标 `paused`。

## 自动化执行铁律（2026-09-15 复盘沉淀）

> 完整实录与提示词模板见 [`自动化执行经验.md`](./自动化执行经验.md)。新开批量执行任务前先读它。

1. **开跑先体检**：端口占用（**9号与19号都用 9333，必须串行**；12号 9334；1号 9371）、目标 Excel 是否被 WPS/Excel 占用、登录态是否有效。
2. **每个会等人的步骤必须有"无输出上限"**：单店/单步骤超过 \~5 分钟没有新日志就按卡住处理，停下来查（读代码/看现场），不许干等，也不许只靠一个总超时兜底。
3. **禁止静默等待**：允许出现"没有输出也没有报错"的卡死属于缺陷，要修代码并补测试（实例：`downloadEventEngine.js` 的人工等待会让 60s 超时被无限推迟，已补墙钟上限）。
4. **验收只认真实产物**，不认脚本自报成功，每项都要给"应有值 vs 实有值"：
   - 9号：应有 9 家店 / 每家 10 行 / 共 90 行 / 日期范围 = 本月1号\~今天-2天 / 金山在线行数 = 本地行数 / 透视筛选 = 最新日期
   - 12号：9 家店成功、金山在线行数 = 本地行数、数据源文件是今天的
   - 5号：**回访只看排队阶段呼损**；IVR 阶段仅当同一客户窗口内**呼入 ≥10 次**才需关注（怀疑不会按数字键转人工）
   - 1号：新鲜度 ≥ 90%
   - 业务侧"业绩/总量比昨天少"要先怀疑"是不是少了某家店"，按店铺清单对账。
5. **首轮失败：先记录+定位，再决定是否手工补跑**（2026-09-17 改，替换旧的"必须补跑"）：失败先写 [`失败台账.md`](./失败台账.md) 并查清原因，**不许程序自动重试、也不许无脑补跑**；确认是"当前已恢复/环境已变"才手工补跑，并在台账写明理由。工具：9号 `node scripts/retryFailedStores.js <任务ID...>`（形如 `pdd-pdd03-all`，不会清空其他店铺，**只在人/模型判定后手工执行**）；12号直接重跑 `scripts/runSummaryAndKdocsSync.js`（当天已成功的店自动跳过）。补跑后必须再核"应有 = 实有"。
6. **修完必须重跑验证**，否则等于没修（实例：天猫2店登录竞态修完重跑一次通过）。
7. **弹窗/广告**：只点"唯一明确关闭入口"；点击期间弹层已自行消失则按已关闭继续；连续多次仍反复出现就停下报错，绝不乱点。
8. **人工验证（滑块/验证码/短信/扫码）停下来叫人**，把窗口留在可见状态；任何会真实提交或发送的动作（发票回传、企微发送）必须先问用户。

