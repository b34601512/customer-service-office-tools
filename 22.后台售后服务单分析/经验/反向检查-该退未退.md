# 反向检查：该退未退（2026-09-18 定）

## 一、用户口径（2026-09-18 拍板）
金山《2026年【湖南怀化售后】对接表》· **退货退款表**里：
- **有记录 = 客户货已退回**（客服已登记）
- **退款状态没填「已退款」 = 客服没处理** → 要提醒
- **提醒范围：只看最近 7 天登记的**（2026-09-18 拍板；更早的老单不提醒）
- 执行方式：**先出清单给用户确认 → 确认后才发企微群**并 @ 当天该值班的人

## 二、实测必须收紧的部分（否则会误报）
| 情况 | 实测数字（2026-09-18） | 处理 |
|---|---|---|
| 退款状态 ≠ 已退款 | 全表 **14876 行** | 太宽：大量换货/维修不涉及退款 |
| 再加「应退金额 > 0」 | 近 7 天 **18 单**（12326 元） | ← 这才是要提醒的 |
| 2025 年历史老单 | 248 单 / 12 万 | 一年多没动，提醒无意义 → 按用户口径排除 |

**结论口径：登记日期在最近 7 天内 且 应退金额 > 0 且 退款状态不含「已退款」。**

## 三、数据坑（都踩过）
1. **日期列（列0）有两种形态**：早期是文本「2025/1/8」，近期是 **Excel 序列号**（46247 ≈ 2026-08/09）→ 必须两种都认。
2. **同一订单会重复登记**（相邻多行同订单号）→ 出清单前**按订单号去重**（实测 974 行 → 511 单）。
3. **客户信息列含换行符**（姓名/电话/地址三段）→ 展示前压成一行。
4. **列索引（0 起）**：0=登记日期、6=购买平台、8=客户信息、10=订单号、11=退货原因、16=客户寄回单号、17=应退金额、19=确认签名、22=退款状态、23=处理客服、25=实际退款金额、26=退款时间。第 22/26 列常被客服填串（出现「1」「待申请」），所以**更要靠应退金额交叉判断**。
5. 表里**行按登记日期升序**，最近的记录在**表尾** → 用 `--start-row` 从尾部扫更快（实测扫 8 万格/8000 行约 8 秒）。

## 四、通知对象（用户 2026-09-18 规则）
- **14:00 前** → 优先 **李守耀（组长）**，没有就 @ 早班值班的售后
- **14:00 后** → @ **晚班值班的售后**（售后晚班 14:00-22:30）
- 值班人来源：排班表 `https://www.kdocs.cn/l/cga7jWGHxzkp`（匿名可读）当月的表，第 2 行是「日期 1 2 3…」，
  售后区块从「售后」标签那行往下（**只有第一行带标签，其余首列为空**），格子直接写「早/晚/年/休」，**不用认底色**。
- 工具：`node src/tools/who-is-on-duty.js`（实测 09-18 → 晚班=缪婷婷）。

## 五、一次完整执行（实测通过）
```bash
node src/tools/kdocs-filter.js --start-row 44000 --limit 3000 --out runtime/kdocs/未退款-表尾.json
# 本地筛：登记日 >= 今天-7天 且 应退>0 且 状态不含已退款，按订单号去重
node src/tools/who-is-on-duty.js                       # 今天该 @ 谁
node src/tools/send-wecom-notice.js --file runtime/kdocs/待发消息.txt --at 缪婷婷            # 预演
node src/tools/send-wecom-notice.js --file runtime/kdocs/待发消息.txt --at 缪婷婷 --send     # 真发（需用户点头）
```
2026-09-18 实发：18 单 / 12,326 元，@缪婷婷，errcode=0。
