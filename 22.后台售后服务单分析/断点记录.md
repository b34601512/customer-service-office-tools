# 断点记录：22.后台售后服务单分析

> 上限 ~40 行。更新 2026-09-18 14:10。经验细节看 `经验/` 两个文档，不写在这里。

## 做到哪了
1. **立项**：只做检查（管理层视角），不代客服处理；先天猫，后扩 4 平台多店铺。
2. **天猫后台采集已打通**：复用 9号 天猫 profile（拷一份即带登录态，实测登录有效，页面显示 德达旗舰店/小黛）。
   数据是**服务端渲染**，直接读 DOM 即可；mtop 接口只是辅助（且多数字段是类型占位符）。
   工具：`src/tools/probe-page.js`（抓页面+JSON+请求清单+HTML）、`src/tools/dump-table.js`（表格结构化）。
3. **金山对接表读取已打通**（复刻 1号 的能力，匿名无头、不需要 token、只读）：
   `src/engine/kdocs.js` + `src/tools/read-kdocs.js`；已读 退货退款表 29412 行 × 34 列（关键列：订单号=10、退款状态=22）。
4. 拿实测单验过：天猫待处理单 `5127667812586099609` 在表里**搜不到** → 符合"能搜到才算漏处理"的口径。

## 下一步（按顺序）
1. **把"待处理售后"一次拿全**：默认视图只渲染 2 行，要搞清分页/切换标签的正确点法（用精确选择器，别用文本匹配）。
2. 用工具串一次真检查：天猫待处理单 → 金山表比对 → 给用户一份"疑似漏处理清单"（含店铺/订单号/状态/天数）。
3. 反向检查（待定）：金山表里 **7413 行"退款状态为空"** 的存量，可能是更大的漏处理池。
4. 跑通后再考虑扩店铺（tmall2）与扩平台。

## 等用户拍板（一次问一个）
1. 判漏口径：**"金山搜到就算漏处理"** 还是 **"搜到且退款状态≠已退款才算"**？
2. 反向检查（表里状态空的存量）要不要纳入检查范围？
3. 汇报格式：清单给到哪（只看结论？还是要每条带 售后单号+订单号+金额+申请时间+超时天数）？

## 常用命令
```bash
node src/tools/probe-page.js --platform tmall --store tmall1 --attach --seconds 10
node src/tools/dump-table.js  --platform tmall --store tmall1 --attach --click "退货退款" --seconds 10
node src/tools/read-kdocs.js  --sheet "退货退款表" --head 5 --out runtime/kdocs/退货退款表.json
```
端口：tmall1=9421、tmall2=9422（窗口常驻，profile 在 `runtime/state/browser-profiles/`）。
