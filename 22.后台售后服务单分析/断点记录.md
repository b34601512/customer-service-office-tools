# 断点记录：22.后台售后服务单分析

> 上限 ~40 行。更新 2026-09-18 15:10。经验细节看 `经验/` 两个文档。

## 做到哪了
1. **立项**：只做检查（管理层视角），不代客服处理；先天猫，后扩 4 平台多店铺。
2. **天猫后台采集已打通**：复用 9号 天猫 profile（拷一份即带登录态，实测有效）。数据是**服务端渲染**，
   读 DOM 即可；`probe-page.js`（页面+请求清单+HTML+全文）／`dump-table.js`（表格结构化）。
   实测拿到当前店 11 条待处理售后（含 售后单号/订单编号/申请时间/类型/状态）。
3. **金山读取已打通两种**：
   - 网页匿名只读（`read-kdocs.js`，复刻 1号 能力）：能读、但**只给当前窗口的数据** ✗
     （退货退款表真实 46503 行，只回 29412 行 —— 用户截图那单在 K46310，正好没读到）。
   - **AirScript 服务端读（推荐）**：脚本 `kdocs-scripts/AirScript-只读查询订单号.txt` +
     `src/engine/kdocsAirScript.js` + `node src/tools/kdocs-query.js <订单号>`；**等用户建脚本给 token**。
4. 官方接口已查实：OpenAPI `GET /api/v1/openapi/ksheet/:file_token/sheets/:idx/cells`（按行分页，
   需 OAuth access_token）；AirScript 走同步 webhook（需 AirScript-Token）。

## 下一步（按顺序）
0. **定期复查判漏**（口径已定）：天猫待处理列表 → 详情倒计时反推签收 → 超 24h 且金山无登记就汇报。
   建议做成一个命令 + 每天定时；用户 09-18 已问过是否要"每天自动跑"（等他回）。
1. **拿金山查询能力**：用户在表里建一次只读 AirScript → 把 webhook + AirScript-Token 写进
   `project-config/kdocs-airscript.json`（不入库）→ 跑 `kdocs-query.js` 验一单（用 K46310 那单
   `5127667812586099609` 做基准：**必须命中第 46310 行**）。
2. **第一份判漏结果已出**（09-18，口径＝用户拍板"金山查不到 + 签收超 24h"）：
   待处理 11 单 → **0 单算漏**（唯一"签收超 24h"的单金山已登记）；**10 单金山未登记**，但 8 单包裹未到、
   2 单签收未满 24h。判据与推算方法写在 `经验/判漏口径.md`（含"剩余≤48h⇒已签收、签收时刻≈现在−（48h−剩余）"）。
   证据：`runtime/tmall/{待处理11单,详情-11单,疑似漏处理清单-2026-09-18}.json`。
   ⚠ 这 10 单会陆续签收 → **必须定期复查**（下一步做自动化）。
3. 反向检查（待用户拍板）：表里「退款状态」空白的存量（7413 行，注意：网页读只覆盖窗口，要用 AirScript 查）。
4. 补跑 tmall2 店铺；再谈其他平台。

## 等用户拍板（一次问一个）
0. **【已完成】金山查询能力打通**（2026-09-18）：AirScript 脚本 v7 已生效，令牌沿用 12号。
   基准验收：查 `5127667812586099609` → **命中退货退款表第 46310 行**（应=实）。三条硬约束记在经验文档第五节
   （传对象不传数组 / 必须 `return main()` / 30 秒上限要分批）。
1. ~~走哪条路~~ **已定：AirScript**；webhook 已拿到并写入配置，**令牌沿用 12号 那份（实测通用）** → 用户侧只差：
   **清空编辑器、重粘 v5 脚本**（`kdocs-scripts/AirScript-只读查询订单号.md`）。已实测：粘贴会吃掉等号序列
   （`===`→`=`、`==`→消失），所以 v5 里**一个等号比较都没有**（用 indexOf+1 取真、只用 < 和 >）。
   粘完我跑 `kdocs-query.js` 验 K46310 那单（返回值里带 `scriptVersion`，可用来确认是否为最新版）。
2. 判漏口径：**"金山搜到就算漏处理"** 还是 **"搜到且退款状态≠已退款"**？

## 设计约定（用户 09-18 问过）
金山脚本：**能力固定、参数驱动**——两个只读脚本（查询订单号 / 筛选未退款），新增检查项优先加参数、不加脚本。
详见 `经验/金山对接表-只读读取.md` 第六节。

## 常用命令
```bash
node src/tools/put-clipboard.js <文件> --show                            # 把文件塞进用户剪贴板（要他粘贴时用这个）
node src/tools/probe-page.js --platform tmall --store tmall1 --attach --seconds 10   # 看后台现状
node src/tools/read-kdocs.js --all --grep "订单号"                                    # 金山跨表搜（窗口读法）
node src/tools/kdocs-query.js 5127667812586099609                                     # 金山服务端查（需 token）
```
端口：tmall1=9421、tmall2=9422（窗口常驻，profile 在 `runtime/state/browser-profiles/`）。

## 京东（2026-09-18 新开）
- 已打通：jd1 登录（`node src/tools/jd-login.js --store jd1`）、售后+纠纷概览（`node src/tools/jd-aftersale-overview.js --store jd1`）。
- 入口：自主售后 `shop.jd.com/jdm/trade/after-sale/independent-after-sale/list`；交易纠纷 `.../trade-dispute/list`（行内带「还剩X小时Y分」）。
- jd1 结论：售后无漏（即将超时 0/待处理 0）；纠纷 2 单待处理但均在时限内（23.3h / 17.5h）。
- **待修**：①纠纷行单号提取 ②纠纷 tab 计数正则 ③首页客服浮层卡片抓取不稳（已改用列表页）。
- **待办**：京东其它店（jd3 DEDAKJ器械店 / jd6 DEDAKJ自营 / jd2）按同样方式接入 stores.json 后跑一遍。

## 拼多多（2026-09-18 新开）
- 已打通 2 家：pdd02 德达医疗旗舰店（9429）、pdd03 DEDAKJ医疗器械官方旗舰店（9430）。命令见 README（`pdd-login.js` / `pdd-aftersale-overview.js`）。
- 判漏：售后工作台顶部「**24小时内将逾期订单数**」=0 无漏；列表行自带 7 天倒计时；「投诉预警」>0 要处理。
- pdd03 列表页被「售后设置」引导拦截 → 用首页「售后过期预警 0」判定（真名也以首页显示为准）。
- **待修**：`pdd-aftersale-overview.js` 首页兜底补丁没生效（兜底块位置不对）。
- **待用户拍板**：pdd02 投诉预警 1 单 + pdd03 待处理工单 3 单，是否各发一条群提醒。
