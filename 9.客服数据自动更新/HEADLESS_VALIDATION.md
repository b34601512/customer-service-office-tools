# 混合无头分支交付验证记录

日期：2026-09-08。分支：`refactor/9-hybrid-headless`。状态：供本地验收，未合并主线。

## 已执行的检查

Windows Server 2025、Node.js 22.23.2、实际安装的 Google Chrome：

| 检查 | 结果 |
| --- | --- |
| 锁定依赖安装 `npm ci` | 通过 |
| 原有回归入口 `npm test` | 通过；金山本地模板集成有14个明确 SKIP，见下文 |
| 新增混合浏览器单元测试 | 25通过、0失败、0跳过 |
| 真实Chrome测试页面集成 | 6通过、0失败、0跳过 |
| `npm audit --json` | 本次返回0个已知依赖漏洞，不代表没有未知漏洞 |

对应[Windows运行日志](https://github.com/b34601512/customer-service-office-tools/actions/runs/34181165353)，通过后提交的源代码为 `d2b13ab1b20aca7d8be54f070f8f4b12f1a1979f`。交付整理只删除临时开发工作流/编辑清单，保留只读回归工作流和验收说明。

六项浏览器测试实际检查：明确广告弹层关闭后业务按钮只点一次；业务导出确认和安全验证不误关；已知广告新页关闭而登录/下载/空白页保留；下载文件落入指定目录；Windows同一账号资料目录从无头重建为可见且测试Cookie保留；CDP正常关闭无头Chrome进程。

页面内容、验证码提示、Cookie和下载文件均为虚构夹具。未登录四个平台的真实店铺，未调用用户的在线金山脚本。不能把测试页面通过等同于真实平台全部通过，也没有得出具体提速比例。

## 本轮修复

人工验证等待不消耗共享弹窗/动作/下载轮询的自动操作时限；浏览器关闭竞态不再误判为连接失败；退出后不重新开窗；异步源文件读取不会把Promise当作已下载文件；连续弹窗有次数和总时限；可能已提交的导出不被弹窗恢复或无头交接重放。

XML依赖 `@xmldom/xmldom` 从0.9.10升级并精确锁定为0.9.12，修复上游已公布的问题，并在升级后的依赖上执行了上述Windows测试。上游参考：[命名空间解析内存问题](https://github.com/xmldom/xmldom/security/advisories/GHSA-965w-775f-mr7g)、[DocumentType校验问题](https://github.com/xmldom/xmldom/security/advisories/GHSA-vr34-hp96-76pp)。不据此推断本项目已发生攻击。

## 金山模板测试的边界

原仓库没有三个本地AirScript `.txt` 模板，干净环境的原测试因ENOENT失败。现在只有依赖模板的14个测试段明确标为SKIP；本地源数据、客户端参数检查和回执持久化仍执行。没有补造模板、删除断言或替换在线脚本。

本机恢复原模板后执行 `npm run test:kdocs-templates`：缺少任何模板就失败；模板齐全时执行所有相关断言。在线金山同步仍须本机验证。

## 本地Codex交接

按同目录 `HEADLESS_TESTING.md` 操作。先保留工作区改动并备份汇总表，再拉分支、安装依赖、跑测试；不要执行会清理本地配置/运行数据的 `git clean -fdx` 或覆盖改动的强制重置。

每个平台各选一家店，对相同日期比较 `npm run cli:headed` 与 `npm run cli:hybrid` 的源报表和汇总结果。重点验收登录失效、验证码、日期切换、弹窗、下载、当天源表复用和单店重跑。

用户确认真实业务测试通过后，再通过PR合并；本次没有向主线写入任何修改。
