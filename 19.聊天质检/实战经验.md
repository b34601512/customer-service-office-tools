# 聊天质检实战经验

## 1. 本项目真正要解决什么

不是“把网页打开”或“抓到一段文本”，而是：

> 从后台真实售前会话中，保留完整上下文，用可复核证据判断客服是否热情、主动跟单、积极解决问题。

因此流程必须是：后台取最新数据 → 保存标准记录 → AI亲自通读完整会话 → 先向用户汇报店铺和客户ID → 与用户确认反馈内容、客服对象和是否发送 → 生成预览；只有用户给出明确确认口令后才发送。

### 固定质检范围

本项目的质检对象严格限定为“售前客服的成交咨询”：客户正在了解产品、比较型号、询问购买条件或表现出购买意向的会话，才进入积极性、答疑和跟单分析。售后故障、物流催件、配件咨询、订单/发票处理、转人工后的服务承接以及其他非购买问题一律跳过，不拿来评价售前成交能力；如会话发生转接，先补齐转接链路，再分别判断是否属于成交咨询。

## 2. 为什么之前会打开到现有 Edge

普通 Edge 已经有自己的主进程。再次执行 `start` 或直接打开 URL，Windows/Edge 往往把请求转发给现有进程；即使命令行写了 `--remote-debugging-port`，现有进程也不会因此变成调试浏览器。

表现通常是：

- 页面能打开，但 9333 没有监听；
- 出现一个 Edge 默认页或恢复页，加上目标后台页；
- 程序无法通过 `/json/list` 看到页面。

## 3. 已验证的独立浏览器做法

9号项目采用的关键做法已迁移到本项目：

1. 为项目使用专用端口 9333，不抢普通 Edge 常用端口 9222；
2. 使用项目自己的 `runtime/browser-profile/`，不复用当前 Edge 资料目录；
3. 使用 Node `child_process.spawn`，`detached: true`、`stdio: 'ignore'`，直接启动可见浏览器；
4. 使用 `--no-first-run`、`--no-default-browser-check`、`--hide-crash-restore-bubble`、`--disable-session-crashed-bubble`；
5. 不使用 `--restore-last-session`，避免恢复旧标签页和旧会话；
6. 启动前检查端口，端口被占用时报告，不擅自结束未知进程；
7. 通过浏览器页面内的 `fetch(..., { credentials: 'include' })` 调京东接口，复用用户手工登录 Cookie；
8. 本机 CDP 请求加入 `NO_PROXY`，避免代理误处理 `127.0.0.1`。

## 4. 正确操作顺序

### 4.1 拉起独立浏览器

```powershell
node src/cli.js browser:start
```

看到独立 Edge 后，用户自己完成登录、验证码和安全验证，并回到：

```text
https://shop.jd.com/jdm/kefu/kf-manage-lite/#/UtilsSetting/ChatLog
```

### 4.2 先列出会话，不要直接猜一条

```powershell
node src/cli.js fetch:list --start 2026-09-09
```

程序会按“京东客服管家”标题或 `kf.jd.com` URL 找目标页；空白页、Edge 新建标签页会被忽略。`fetch:list` 默认只列岗位为“售前”的会话；如确需抽查售后，必须显式使用 `--role 售后` 或 `--role all`。

京东会话存在“售前转售后”链路：转接后原售前不再继续回复，可能是正常分工，不能直接判为漏回复或跟单不积极。发现转接线索时，必须追加 `--role all`（或单独读取售后记录）补齐接手链路，并分别评价：转接前售前是否及时答疑、是否说明转接原因和交接内容；转接后售后是否及时承接、解决问题并形成闭环。当前记录没有明确转接标记、接手人或后续记录时，只能标记“转接链路待核实”，不得猜测责任。

### 4.3 保存指定会话

```powershell
node src/cli.js fetch:save <sid> --start 2026-09-09
```

保存后的标准格式是：

```json
{
  "format": "chat-record/1",
  "meta": { "customer": "脱敏ID", "window": "YYYY-MM-DD", "store": "" },
  "messages": [
    { "time": "...", "role": "customer", "text": "..." },
    { "time": "...", "role": "waiter", "text": "..." }
  ]
}
```

### 4.4 人工通读并质检

程序不提取响应时延、关键词、连续消息或“热情/跟单”信号。最终判断必须由 AI 亲自回看完整聊天：

- 热情：有没有承接情绪、礼貌回应、让客户感到被认真对待；
- 主动跟单：有没有追问有效需求、主动提供下一步、客户没继续问时仍有效推进；
- 积极解决：有没有查库存、算价格、申请优惠、给替代方案等具体动作。

### 4.5 反馈前后顺序

给用户反馈时，先交代：店铺名称、客户 ID、客服后台昵称和对应客服姓名，再给出逐条证据和建议。用户确认文案后，先用预览工具核对目标客服和@方式，最后才讨论是否发送。

本次真实验证的关键结果：京东记录中的后台昵称可以通过9号项目映射到 canonical 客服；1号项目当前没有配置 userid，因此使用手机号底部@；用户确认后发送返回企微 `errcode=0`。

## 5. 当前质检口径

### 热情

证据重点：

- 是否先回应客户感受，而不是直接甩结论；
- 语气是否完整、自然、愿意帮助；
- 是否出现明显冷处理、敷衍或机械重复。

不能只因为出现“您好”就判定热情，也不能只因为出现“哦”就判定冷淡，要结合上下文。

### 主动跟单

有效主动跟单通常包括：

- 追问规格、数量、预算、使用场景等关键需求；
- 主动核价、查库存、给链接或提供方案；
- 客户表达犹豫后，主动给出下一步选择；
- 客户暂时没有追问时，补充有价值的信息并推动决策。

连续发两条消息只是“主动信号”，如果只是重复“现在就是这个价格”，不能算有效跟单。

### 积极解决

重点看是否把问题向前推进：

- 只解释现状：积极性偏弱；
- 解释后给出可执行方案：积极性较好；
- 给方案并确认客户下一步：积极性更强。

## 6. 不允许的做法

- 不用旧的 `runtime/chat` 记录冒充今天后台最新数据；
- 不因为一条会话就评价整个客服；
- 不把响应快直接等同于热情；
- 不把优惠直接等同于主动跟单；
- 不把关键词统计直接等同于 AI/主管结论；
- 不把客户 ID、姓名、Cookie、密码写入源码或文档；
- 不擅自杀掉未知端口进程，尤其不要结束用户现有浏览器。

## 7. 企业微信反馈工具

### 7.1 1号项目的实际机制

1号项目的通知链已只读复核：

- 正文优先使用企微官方 `<@userid>` 行内艾特；
- 没有 userid 时，把手机号放入 `mentioned_mobile_list` 做底部@；
- webhook 请求体是 `msgtype: "text"` + `text.content` + `mentioned_mobile_list`；
- 发送有超时和一次重试。

岗位映射也必须保留：1号从成员接口的显示名中解析“姓名 + 售前/售后”，9号的 `personMappings` 保存“后台昵称 → 汇总姓名 → 岗位”。19号把两者合并到 `nicknameMappings`，选取会话时默认只允许“售前”，不能把已映射为售后的昵称当成售前样本。

19号复刻为 `src/services/wecomFeedback.js`：客服昵称来自9号项目映射，企微成员目录来自1号项目；`wecom:preview` 只构造预览，不联网。

多行反馈建议使用：

```powershell
node src/cli.js wecom:preview <聊天文件名> --content-file <文案文件>
```

代码调用时使用 `contentLines` 或 `joinFeedbackLines()`，不要在多层命令字符串里手写转义换行。

### 7.2 发送边界

- 反馈原文必须由用户与 AI/人工先沟通确定；本项目不自动生成评价。
- 是否发送必须由用户明确决定；当前 CLI 不提供自动发送命令。
- 底层发送函数要求明确确认口令；本次已在用户确认后成功发送一次，未确认时测试证明不会触发网络请求。
- 发送调用方还必须提供工作区和报告上下文；企微成功返回后，原文才写入 `runtime/reports/YYYY-MM-DD.md`，报告写入失败会明确提示“已发送但记录失败”，不得重发。

### 7.3 换行踩坑

1号项目的 `buildTransferReminderMessage()` 使用 `lines.join("\n")` 生成真实换行，`wecomRobot.js` 直接把 `input.content` 放入 JSON 请求体。`JSON.stringify()` 在调试输出里显示 `\n` 是正常的 JSON 表示；如果群里显示的是字面量 `\n`，说明发送前的字符串本身就包含反斜杠和字母 n。

本次曾因 PowerShell → Node → JavaScript 多层转义，多转义了一层，导致群里出现字面量 `\n`。以后按以下规则执行：

- 多行文案优先写文件，用 `--content-file` 读取；
- 程序内部用 `lines.join("\n")` 或 `joinFeedbackLines()`；
- 不把 `\\n` 当作最终文案内容传给 webhook；
- 发送前预览应检查字符串含真实换行，而不是只看 JSON 打印结果。

### 7.4 两种@方式不能混用

- `bottom-mobile`：正文不写客服@，只把手机号放入 `mentioned_mobile_list`，企业微信会自动在消息底部显示@。
- `inline-userid`：正文使用 `<@userid>`，`mentioned_mobile_list` 保持为空。
- 两种方式不能同时写；19号工具现在会拒绝“底部手机号@ + 正文客服@”，防止重复艾特。

当前模板（2026-09-23 用户拍板改版，见 7.5）：

```text
【京东1店】客户ID：<客户ID>
<一句话，按非暴力沟通：观察（不带评判的事实）→感受→需要→建议；句式“假如我是客户，我感觉……，建议后续……”，不贴标签不指责，别啯嗦>
```

审批给用户看的正文也按这个模板展示，不额外添加 `@客服姓名` 行；真正的@由发送层根据映射完成。只有切换到行内 userid@时，才在正文中出现 `<@userid>`。

### 7.5 反馈正文格式（2026-09-23 用户拍板改版，替代 2026-09-10 “话术＋为什么”分块）

- **第一行**：店铺＋客户ID 同一行，如 `【京东1店】客户ID：吉林惠生活`。
- **第二行**：直接说问题，**只能说一句话**，按**非暴力沟通**写：观察（不带评判的事实）→ 感受 → 需要 → 建议，句式固定：**“假如我是客户，我感觉……，建议后续……”**；**不贴标签不评判**（如“懒得搭理”）、**不用威胁口吻**（如“那就给差评”），也**不直接指责客服**；别啯嗦，不再要【售前跟进建议】小标题和事实长段。
- **后续反馈都按本格式说**（2026-09-23 用户拍板）。
- **不再规定**“话术：…／为什么：…”分块；不强制给可复制话术，不写“为什么”行，拿不准时按当次用户给的措辞走。
- 需要客服用现成话术时，指明去**客服宝话术管理软件**搜关键词（如“养鱼”）；不要让客户去搜，也不把大段话术复制进正文。
- 正文仍不写客服@（底部手机号@模式），@只由发送层完成；用户逐字确认＋口令 `161323` 才发送的流程不变。
- 2026-09-10～09-23 按旧格式已发送的不重发、不撤回。

## 8. 迁移来源

| 目标能力 | 来源 | 迁移结果 |
|---|---|---|
| 京东接口取数 | 13号 `src/services/jdFetch.js` | 保留并加入本机代理绕过 |
| 京东原始记录转换 | 13号 `src/services/jdConvert.js` | 保留 |
| 标准聊天校验与存储 | 13号 `chatSchema.js`、`chatStore.js` | 保留 |
| txt/json 导入 | 13号 `importers.js` | 保留 |
| 独立浏览器启动 | 9号 `chromeLaunchArgs.js`、`chromeLauncher.js` 思路 | 重写为本项目单一 `browserSession.js` |
| 企业微信@与预览 | 1号 `wecomRobot.js`、`wecomTextMention.js` + 9号姓名映射 | 只构造预览，不自动分析、不发送 |
| 多行反馈 | 1号 `lines.join("\n")` | 19号增加 `content-file` 与 `joinFeedbackLines()`，避免多层命令转义 |

## 9. 后续扩展顺序

1. 先稳定真实取数和人工抽查；
2. 再积累不同售前场景样本；
3. 再让 AI 按固定证据格式输出质检结论；
4. 最后才考虑汇总到客服维度，且必须保留样本量和原始证据。

## 10. 2026-09-09 接力状态

### 10.1 今天已经完成

- 已用独立 Edge（项目端口 9333）从京东后台取数；当前 `fetch:list` 默认只显示售前。
- 已人工通读 3 条正常售前购买咨询，其中 2 条已按用户确认文案发送企微反馈，1 条只完成评价、未发送。
- 另有 1 条售后记录、1 条配件/非正常制氧机购买记录，均已移到项目外的可恢复备份，不再作为今天的售前样本。
- 曾尝试保存一条最新记录但未形成标准文件；它不算已抽查，下次必须重新取最新列表。

### 10.2 下次打开窗口先做什么

1. 先确认独立浏览器和登录状态；必要时运行 `node src/cli.js browser:start`。
2. 运行 `node src/cli.js fetch:list --start YYYY-MM-DD --role 售前`，以当天实际日期替换，不复用旧列表。
3. 只选择正常购买制氧机的售前会话，再运行 `fetch:save <sid> --start YYYY-MM-DD --role 售前`。
4. AI 必须亲自通读完整记录；向用户只汇报店铺、客户 ID、后台昵称与映射客服、证据和建议，不再重复粘贴完整原文，原文可在浏览器或标准文件中查看。

### 10.3 今日新增判断口径

- 记录正在接待时，不能仅因当前记录暂时结束就判定“未跟进”；要结合最后一条消息是谁发的和时间上下文。
- 客服最后发言后客户未继续回复，也不能倒推客服没有跟进；评价重点是客服是否主动追问需求、给方案、推动下一步。
- 京东会话若出现转接售后，原售前停止回复可能是正常分工；先核对转接事件、接手角色和转接后的完整记录，再判断是否漏回复。转接前后的客服必须分开评价，不能把转接后的等待时间算到原售前头上。
- 只有看到完整转接链路和接手记录，才能判断售后是否承接；缺少转接证据时统一写“转接链路待核实”，不把推测写成质检结论。
- 只看售前关于成交咨询的会话；售后、物流、配件、订单/发票处理、转人工服务承接和非购买问题直接跳过，并说明跳过原因。
- 评价必须区分“回复及时”“答复正确”“热情承接”“主动跟单”“确认下单”，不能用单一关键词或消息数量代替判断。

### 10.4 企微反馈接力规则

- 先把拟发的完整文案发给用户；用户确认内容后，再要求确认口令 `161323`，未得到口令不得发送。
- 发送内容必须与用户确认稿逐字一致，不擅自补充时间、结论或免责声明。
- 当前成员目录主要使用底部手机号@：正文不要再写 `@客服`；只有行内 userid@模式才写 `<@userid>`。两种方式不能混用。
- 多行文案优先使用 `--content-file`；`wecom:preview` 只预览、不发送。
- 今天早期有一条旧文案曾同时使用正文@和底部@，可能造成重复艾特；现在工具已加硬性拦截。除非用户重新明确要求，不重发、不撤回已发送消息。

### 10.5 当前不要做的事

- 不恢复或继续使用已移出的误选记录作为售前样本。
- 不新增第二份“经验/交接”文档；后续只更新本文件。
- 不把脚本统计、关键词、时延自动转换成客服评价，也不自动发送反馈。

### 10.6 工作量报告记录

- 每次用户确认并成功发送的反馈，自动追加到 `runtime/reports/YYYY-MM-DD.md`。
- 报告记录店铺、客户 ID、来源聊天文件、后台昵称与映射客服、岗位、@方式、发送时间、企微结果和用户确认的反馈正文；不记录 webhook 地址或确认口令。
- 调用 `sendWecomTextMessage` 时必须同时传 `reportWorkspace: ws` 和 `report`（至少含 `workDate`、`store`、`customerId`、`staffName`、`approvedContent`）；缺少报告上下文时，工具会在联网前拒绝发送。
- `node src/cli.js report:show --date YYYY-MM-DD` 可查看当天报告，报告内容就是给上级核对“当天做了哪些质检、给了哪些跟进建议”的工作量证明。
- 发送成功但报告落盘失败时，消息可能已经到群里；工具会明确提示“不要重发”，先补报告记录或人工核对，避免重复艾特。
- 已按可核对的完整确认稿补录一条历史发送记录，并明确标注发送时分未留存；另一条只有“已发送”事实、缺少确认原文，仍不追猜、不补写。新发送从本机制启用后自动留痕。

## 11. 2026-09-10 接力状态

### 11.1 今天已经完成

- 独立浏览器（9333）在线，已从京东后台 `fetch:list --start 2026-09-10 --role 售前` 拉到 31 个售前会话；上午 10:55–11:01 已保存 19 条到 `runtime/chat/`，并按纪律跳过售后、配件、发票/订单处理等非售前购买咨询。
- 已亲自通读 19 条记录并按新格式发出 3 条反馈，均 `errcode=0`，入档 `runtime/reports/2026-09-10.md`：
  - jd_7c290a2d3af2f（安安→叶炳辉）：标杆跟单——追问血氧/疾病、分级推荐机型、主动申请优惠。
  - jd_yzsleyidvvcy（璇璇→韩欢欢）：已下单、赠品换血氧仪即时备注回执，态度热情。
  - 牧杨人1986（璇璇→韩欢欢）：已下单要送人，先事实后建议（话术+为什么）。
- 2026-09-10 起反馈正文采用 7.5 分块格式（事实 / 建议：话术＋为什么）；旧格式已发出的不重发不撤回。

### 11.2 待办与候选

- 未发送：jd_666364f5c4bad（韩欢欢，催物流处理完整，用户已认可旧文案但未发送，可按新格式重排再确认）。
- 未起草：改进建议类 jd_661f1add676fa（客户 00:26 问“声音怎么那么大”，08:07 才收到模板欢迎词、问题未答）；jd_bzmukchoycis（“呼吸困难憋得不行”只回轻度缺氧模板，未追问未推荐）可同批。
- 用户未选：jd_422097110b23e、jd_55fc2739cdb08（不错但偏简单，待用户定）。
- 其余 19 条中的轻量问答（jd_782d/715/zhao0044/xiaosionline/488/539/135922/bjwanxufeng/zkfzil/ldpqhlwaejsb）判定为基本到位或议题较轻，未单独起草。

### 11.3 下次打开窗口先做什么

1. 同 10.2：确认独立浏览器与登录，重新 `fetch:list --start <当天> --role 售前`，不复用旧列表。
2. 新记录用 7.5 格式起草（以 7.5 当前版本为准；2026-09-23 起为两行简洁版）。
3. 每条与用户逐条确认后再 `wecom:preview`，口令 `161323` 后才发送；发送脚本用完即删。

## 12. 2026-09-12 接力状态

### 12.1 今天已完成

- `fetch:list --start 2026-09-12 --role 售前` 拉到 29 条售前会话（实时增长），已全部保存到 `runtime/chat/`；同一客户多段会话用 `__<sid前8位>` 后缀防覆盖。
- AI 已亲自通读全部 29 条文本，并额外按“客户维度”全量重查，核实跨段完整链路。结论底稿：`runtime/outputs/raw-2026-09-12.txt`。

### 12.2 今日质检结论（店铺：德达官方旗舰店）

在岗：安安→叶炳辉（售前）、璇璇→韩欢欢（售前）；售后接手：德德→李守耀、香香→邓远祥；`jimi_vender_63530` 为夜间自动客服。

正面样本：
- 安安：wdnupnqivjpdjjj（轻微肺梗阻→推荐 1L＋“放心拍”）；jd_68a9af5da8b5c（肺癌→推 5L 医用并发链接）；532胶州市赵芳（分贝＋加水两问全答）；jd_nofrhvwriflt（换水/维护答全）。
- 璇璇：jd_56b27830e896b（追问病情→按肺气肿推 5L 并解释原因）；jd_7007e7ecbee03（如实说不能医用级并转推医用机）；质保、耗材、加水/加药类均答全。

问题候选（按证据强度）：
1. 璇璇 / jd_zhzeokjwfnzi（11:28）：客户“我着急用”“需要明天送到”，仅回“显示的时间就是最快了”，未说明为何已是最快、未给准信；13:42 已按用户确认稿发送（errcode=0，入档 runtime/reports/2026-09-12.md）。
2. 安安 / jd_wzccfvxtegfj（09:44、09:52）：客户问“能24小时连续用吗”被 template2 原样回显、未答；“西藏可以用400多那个吗”只回表情（引用旧回复），显得不耐烦、未给准信；13:52 已按用户确认稿发送（errcode=0）。
3. 璇璇 / jd_56b27830e896b（11:05）：客户问“德达497的能用吗”被重复的“问病情”套话带过；11:10 后无促单动作。
4. 轻：璇璇 / jd_6abe63d1087a2（重问“需要加水吗”只回表情）；安安 / jd_7486726aade9b（首答错模板“免加水”，后解释未正面确认可放药）。
5. 共性：全天几乎无主动促单/跟单，仅“放心拍”2 次；高意向会话（肺癌 5L、轻微肺梗阻、“好”）客户沉默后均无二次跟进，全部由 10 分钟超时自动关闭。

已按规则跳过：物流催件（zengjianhuan1987×2 段已转售后）；夜间转人工留言（00:15–04:38 共 3 段，夜班机器人应答、早班问候）；售后故障/退货（pennyyuping 血氧仪 4 段、jd_70d0f1e2b89fc 新机异味）；配件购买（jd_hwnmlsxfkaiq，已由售后香香接手）。

### 12.3 今日新增经验（工具与判断）

- 京东按段拆包：同一客户一天可能有多段 sid（售前段 / 夜间机器人段 / 售后接手段），段末带“人工转接/客服离线”关闭标记。只读单段容易误判“未回复、未给视频”；判断前必须按客户维度取全量并看 `closeTypeDesc`。
- 附件不进文本：`type` 可能是 `video`/`image`/`template2`；视频以空文本呈现、只在 `imgUrl` 有地址，标准记录文本层看不到，不能凭保存文件断言“没发视频”。
- `#E-s21/#E-s31/#E-s33` 等是京东表情代码；单独出现等于没有文字答复，跟在文字后才算表情补充。
- 客服昵称会带不同前缀（`--安安` / `-德德`），映射前先去掉“德达官方旗舰店”前后缀。
- 店铺配送口径（用户确认）：本店用京东快递、就近仓发货，已是全网最快、网购即最优。客户说“明天必须到”而页面显示更晚时，正确做法是讲清“已经最快”＋给准信（明天赶不上、最早周一），不要让客户让步时间；“帮您查有没有更快的”这类建议不成立。
- 批量按客户查询接口会限流/偶发空返回；需加延迟与重试，不能把一次空结果当作“无会话”。

### 12.4 下次打开窗口先做什么

1. 与用户逐条确认候选反馈（12.2 的 1/2/3），按 7.5 格式起草 → `wecom:preview` → 口令 `161323` 后才发送。
2. 若继续跟踪成交：先看 jd_68a9af5da8b5c、wdnupnqivjpdjjj、jd_56b27830e896b 下午是否回访或下单。

### 12.5 工具待办（未改代码）

- `writeChat` 文件名只按“日期＋客户”，同日同客户多段会互相覆盖；今天用外部改名规避，后续可考虑文件名纳入 sid 尾号。
- `runtime/outputs/raw-*.txt` 是跨段核对底稿，属 runtime 数据，不入仓库。

## 13. 2026-09-23 补充经验

- **数据完整性已核对**：页面 UI 的 `waiterSession/jingmai/queryList?viewMode=1`（会话维度）与程序的 `chatLog/queryList.action` 同日逐条一致（2026-09-23：81=81，差集为空）。程序直接带日期参数请求，**不受页面筛选/搜索状态影响**；页面停在“某客户搜索”上不代表程序只取了该客户。viewMode=0/2 是客户维度（当日 65 用户），不能拿来跟会话数对账。
- 反馈正文格式已改版，见 7.5（非暴力沟通句式：假如我是客户，我感觉……，建议后续……）。
- `fetch:list` 无输出静默退出的根因与修复：见仓库根 `失败台账.md` 2026-09-23「19号」条（窗口最小化→后台节流→页内 JS 冻结）。
