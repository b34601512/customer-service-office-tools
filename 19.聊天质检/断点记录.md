# 19号断点记录

## 2026-09-23（14:50，两窗口接力后合并）

- 当日质检完成：全量 64 段、售前 **36 段**（安安 17／小棠 19）逐段通读并补齐转接链路。
  报告：`runtime/outputs/质检报告-2026-09-23-售前.md`（第五节=14:42 后新增补录）；底稿 `raw-2026-09-23.txt`。均 runtime 私有，不入库。
- 最重问题：吉林惠生活怼客（"您直接去买鱼打氧的不就行了吗"）；jd_714df42b859a2 原样回显漏答；
  jd_79fb96658ec82 开场"在地址"。**本轮未起草、未发送任何企微反馈。**
- 下一步：等用户点案例+确认文案 → `wecom:preview` → 仅口令 `161323` 后才发送；
  晚间复查新增段（jd_nyvjepxkwfua 14:43 仍在聊）。
- 已修缺陷（提交 `e60e09f`，npm test 19/19）：Edge 窗口最小化→后台标签被浏览器节流→页内 JS 冻结→
  `fetch:list` 60s 后**无输出静默退出 0**；现在超时/断连显式报错并提示"把窗口还原到前台"。
- 修正旧说法（证据见 `失败台账.md` 2026-09-23「19号 fetch:list」条）：无输出**不是**"岗位映射未命中"
  （那种情况会打印 `✗ 未查到会话`），是页内 JS 被节流。**开跑先看窗口是否最小化。**
- 常用取数：`node src/cli.js fetch:list --start YYYY-MM-DD --role 售前`；
  `writeChat` 同客户多段互覆，存后按 `__<sid前8位>` 改名（writeChat 本身尚无 sid 后缀，工具待办）。
- 本会话托管 loop：#1（idle，maxFires=8）。
