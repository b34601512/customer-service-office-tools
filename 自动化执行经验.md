# 自动化执行经验 · 长时间批量任务（2026-09-15 实录）

> 本文记录 2026-09-15 一次「多项目自动化批量执行」中真实踩到的坑与可复用的写法。
> 目的是让**下一次（人或 AI）执行这类任务时不再重复踩坑**，也可直接复制文末的提示词模板。

---

## 一、这次到底卡在哪里（事实链）

### 现象
`9.客服数据自动更新` 的整轮汇总（9 家店）在 **08:53:47** 打印最后一行

```
[拼多多下载][等待文件落盘] 平台已确认导出，正在等待新 Excel 文件
```

然后**整整 43 分钟没有任何输出**，既不报错也不推进，直到外部看门狗（监控任务的"无输出超时"）把它杀掉。
杀掉时结果只是「2/9 家成功」，抖音两家店和金山同步根本没跑到。

### 直接诱因
拼多多的导出只落下一个残缺文件：

```
runtime/output/downloads/pdd/pdd03/GY7cCAlKO2o8FfLCbMdcSA客服统计20260915 08-53-46.xls.crdownload   (12288 字节)
```

同一个目录里还躺着 **09-12、09-14 的同类 `.crdownload` 残片** —— 说明"下载只写了个头就停住"不是偶发，而是**反复出现**的老问题（浏览器下载没有完成，Chrome 保留了 `.crdownload` 临时文件）。

### 真正让它变成"静默卡死"的代码缺陷（已修）
`9.客服数据自动更新/src/shared/downloadEventEngine.js` 的等待循环：

```js
const deadline = getAutomationTime() + safeTimeoutMs;   // 60s
while (true) {
  const artifact = await readArtifact();
  if (artifact) return artifact;
  await checkBrowserHumanRequirement();                 // ← 人工守卫：耗时会被记入 humanWaitMs
  const remainingMs = deadline - getAutomationTime();
  if (remainingMs <= 0) return null;                    // ← 只有一个"自动时间"上限
  await sleep(...);
}
```

而 `getAutomationTime() = Date.now() - scope.humanWaitMs`。

设计本意是对的（人工解验证码的时间不该烧掉下载时限），但组合起来产生了一个**没有上界的等待**：
只要人工守卫反复"吃掉"墙钟时间并把它记进 `humanWaitMs`，`getAutomationTime()` 就几乎不前进，
`remainingMs` 永远大于 0 → **名义 60 秒的超时可以被无限推迟**。

**修复**（`downloadEventEngine.js`）：在自动时限之外再兜一层**墙钟上限**
`墙钟上限 = 下载时限 + 人工验证预算(humanTimeoutMs)`，到点必须返回 `null`，
把"静默等待"变成"可见失败"（调用方随即抛出「60 秒内没有检测到新文件」）。
配套测试：`tests/downloadWaitWallClockCap.test.js`。

### 为什么这次损失被放大了
1. **失败不是错误，是沉默**：没有错误就没有重试、没有告警、没有推进，只能靠人盯着。
2. **浏览器中途也退出了**：卡死期间调试浏览器（9333）自己没了，现场无法再核对。
3. **批次内没有"每店必须产出"的硬约束**：一家店卡住 = 整轮停摆（后面 2 家店 + 金山同步全部没跑）。
4. **首轮失败不少见**：这一轮 9 家店首跑只有 2 家成功（天猫2店登录竞态、京东1/6店调试浏览器被关、
   拼多多2店下载超时、抖音2店没跑到）。
   > ⚠️ **2026-09-17 方针变更**：不再“失败就程序重试/无脑补跑”。失败一律先写 `失败台账.md` 并**查清原因**，
   > 由人/模型判定后再**手工补跑**（台账写明理由）。“补跑 + 对账”环节仍要预留，但它是**人的决定+手工执行**，不是程序自动重试。

---

## 二、下次执行这类任务的正确姿势（可直接写进提示词）

### 1) 先体检，再开跑
- 端口占用：**9号与19号都用 9333**（互相冲突，必须串行）；12号用 9334；1号用 9371。
- 目标 Excel 是否被 WPS/Excel 占用 → 用 `openSync(path,'r+')` 试写即可判断。
- 登录态是否有效 → 看上次运行日志/跑一次只读命令确认。

### 2) 每个"可能等人"的步骤都要有"无输出上限"
- 不要只给整任务一个总超时。要求：**单个子步骤/SKU 超过 N 分钟没有新日志，就按失败处理**。
- 这次就是用监控的"无输出超时"（420s）才把卡死揪出来的；否则会一直挂着。

### 3) 验收必须对"真实产物"和"应有清单"，不能只看脚本 OK
- 反例：脚本最后打印了 `P4_RETRY=SUCCESS`，但当时只有 8/9 家店。
- 正例：核对「**应有 9 家店 / 每家 10 行 / 共 90 行 / 日期范围 = 本月1号~今天-2天**」，
  再核对「金山在线行数 = 本地行数」「透视筛选 = 最新日期」。
- **总业绩变少**这类业务异常，第一反应就应该是"是不是少了某家店"，然后按店铺清单对账。

### 4) 失败先记录+定位，再由人/模型决定是否手工补跑（2026-09-17 改）

- **程序不重试**：不允许“重复请求平台数据”的自动重试（开关/参数/死代码都不留）。
- 失败先写 `失败台账.md`：日期/项目/店铺/步骤/现象原文/等待时长/证据路径/判定/处置。
- **不许猜**（“可能是限流/抖动”这类话在只有 1 次现象时不许说）；**同一现象 ≥2 次才算规律**，
  成了规律才提程序级优化（并写清根源方案）。同一轮连环补跑只算 **1 次事件**。
- 确认"当前已恢复/环境变了"才手工补跑，并写明理由（为什么不是白跑）：
  - 9号：`node scripts/retryFailedStores.js <任务ID...>`（任务ID 形如 `pdd-pdd03-all`，**不会清空其他店铺数据**，**只在人/模型判定后手工执行**）
  - 12号：直接重跑 `node scripts/runSummaryAndKdocsSync.js`，**当天已成功的店会自动跳过**，只补失败的
- 补跑后**再对一次账**，直到"应有 = 实有"。

> 反面教训（2026-09-17）：天猫2店平响报表卡“加载中”，我们 20 分钟内连试 3 次（每次干等 180 秒）全白等；
> 而一次只读诊断（2 分钟）就把真相定了——报表本身能加载，是平台侧时间窗口性异常。
> **自动重试不仅白等，还会把证据抹掉**（重试成功那次失败就没人记，规律永远浮现不出来）。

### 5) 修完必须重跑验证（否则等于没修）
- 这次 天猫2店 的登录竞态修完立刻重跑 → 一次通过，才算真的修好。

### 6) 遇到人工验证要"停"而不是"猜"
- 短信/滑块/扫码：把浏览器窗口**留在可见状态**，直接叫人；不要反复重试硬闯。
- 需要人拍板的业务口径、任何会真实提交/发送的动作，一律先问。

### 7) 收工留痕
- 写 `断点记录.md`（做到哪 / 下一步 / 待你拍板 / 常用命令）。
- 代码改动当场 commit + push（业务数据、`runtime/`、`*.xlsx` 不入库）。

---

## 三、提示词模板（复制即用）

```text
【任务】按照下面的顺序执行 N 个项目，逐个验收并汇报。

【硬性要求】
1. 先体检：端口占用（9号/19号都用9333，必须串行；12号9334；1号9371）、目标Excel是否被WPS/Excel占用、登录态是否有效。
2. 长任务必须后台跑并带“无输出上限”：单个子步骤/单店超过 5 分钟没有新日志，就判定为卡住——停下来查明原因（读代码/看现场），
   不许干等，也不许只靠一个总超时兜底。
3. 失败要变成“可见的错误”：不允许出现“没有输出、也没有报错”的静默等待；发现这类缺陷就修代码并补测试。
4. 验收只认真实产物，不认脚本自报成功。每一项都要给出“应有值 vs 实有值”：
   - 9号：应有 9 家店 / 每家 10 行 / 共 90 行 / 日期范围 = 本月1号~今天-2天 / 金山在线行数 = 本地行数 / 透视筛选 = 最新日期
   - 12号：应有 9 家店 成功、金山在线行数 = 本地行数、数据源文件是今天的
   - 1号：新鲜度 ≥ 90%
5. 失败后**不要自动重试**（程序里不允许“重复请求平台数据”的自动重试）：先写 `失败台账.md` 记录现场
   （哪家店/缺哪张表/卡在哪一步/等了多久/页面地址/原始报错），**查清原因后再由你判定是否手工补跑**并写明理由；
   只有 1 次现象不许下结论（同一现象 ≥2 次才算规律）。手工补跑入口：9号 `scripts/retryFailedStores.js`
   （会自动跳过已成功的店），12号重跑 `scripts/runSummaryAndKdocsSync.js`；补跑后再对一次账直到“应有 = 实有”，
   并把“失败清单 + 原因 + 是否/为何补跑 + 结果”写进汇报。
6. 业务口径、滑块/验证码/短信登录、任何会真实提交或发送的动作（发票回传、企微发送）——停下来问我，不要猜、不要擅自做。
7. 弹窗/广告：优先复用项目已有的弹窗治理能力；只在“唯一明确关闭入口”上操作；
   若点击期间弹层已自行消失，按已关闭继续（不要因此判整店失败）；连续 N 次仍反复出现就停下报错，绝不乱点。
8. 每完成一块就 commit + push（只提交源码，业务数据/runtime/*.xlsx 不入库），并覆盖更新 断点记录.md。
9. 最后把我需要人工验证的界面以只读方式打开，并给出“验收清单”（逐项打勾 + 关键数字 + 产物路径 + 待我拍板的事）。
```

---

## 四、本次沉淀的代码修复清单

| 项目 | 修复 | 提交 |
|---|---|---|
| 9号 | 天猫登录面竞态（`Execution context was destroyed`）不再让整店失败 | `7ed6f54` |
| 9号 | 下载等待补**墙钟上限**，杜绝“人工等待让超时无限推迟”的静默卡死 | 本次 |
| 12号 | 弹窗点击期间弹层自行消失 → 按已关闭继续；连续 3 次仍有遮挡层则有界停下 | 本次 |

## 五、可复用的内部工具（本次新增）

```bash
# 9号：全部汇总 + 金山同步 + 透视筛选（含启动日期重算）
cd "9.客服数据自动更新" && node scripts/runSummaryAndKdocsSync.js
# 9号：只补指定店铺（不会清空其他店铺数据）
cd "9.客服数据自动更新" && node scripts/retryFailedStores.js jd-jd1-all pdd-pdd03-all douyin-douyin2-all
# 12号：全部汇总 + 金山一键同步数据源（已成功的店自动跳过）
cd "12.店铺指标数据自动更新" && node scripts/runSummaryAndKdocsSync.js
# 5号：今天+昨天的排队呼损只读查询
cd "5.电话漏接分析/电话漏接分析后台" && python scripts/query_queue_loss.py
```
