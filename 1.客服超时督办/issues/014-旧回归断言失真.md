# 旧回归断言失真

## 证据
重构前 HEAD 隔离测试已复现三个失败：两个通知测试给客服与主管相同手机，却要求重复 @；一个报表测试要求已被产品移除的“平均”说明。生产代码明确手机号去重、报表只显示累计。

## 验收与边界
- [x] 两个独立成员测试改用不同虚构手机号，另增加同号去重测试。
- [x] 报表仍验证现有累计封顶提示、列宽、范围和排序，不放松为仅不报错。
- 不为了旧断言改变生产业务规则。
