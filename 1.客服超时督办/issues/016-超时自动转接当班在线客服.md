# 超时自动转接当班在线客服

## 最终口径（用户 2026-09-16 定稿，最朴素的版本）

> 用户原话：“简单的口径就是售前转售前，售后转售后，运营的接待算售前。无论是售前还是售后，只要有人在线就可以转，只不过硬规则是只转当班上线了的客服，否则转了没人回复。”
> “核心关键是客户的信息有人回复就这么简单。”

1. **触发**：首次超时 / 10 倍漏回复提醒到点，且**原接待已不在自己班次的时间窗内**（运营账号一律按不在班）。
   - 在班时间窗：早班 `08:00~16:30`；售前晚班 `15:45~23:45`；售后晚班 `14:00~22:30`。
   - 早晚班重叠期（售后 14:00~16:30、售前 15:45~16:30）里早班人还没下班，**不算不在班**。
2. **目标**：**同组**（售前→售前、售后→售后、运营→售前）+ **当天当班**（本人班次时间窗覆盖当前时刻）+ **已上线**（售前看「可被转接」，售后看「自动分配」），按排班表从上到下取第一个满足的。
3. **不看值班背景色、不看组长、不看值班标记**：排班表颜色只用于 `排班颜色清单` 诊断日志，不参与选人。
4. **当班的人都不可接**（都没上线 / 没人排这个班）→ **不转** 并 **@主管**；非工作时段不转也不发群。
5. **指令发出 ≠ 成功**：发 socket 帧后按联系人快照核对 `assignedTo` 真的变成目标才算成功，成功发群简报、失败@主管。

> 这条口径取代了此前基于“值班人（浅绿/浅蓝背景色、售后组长在班）”的选人规则——那些规则和用户真实需求（客户消息必须有人回）对齐度不够，已从代码中删除。

## 意图

运营/售前/售后负责接待的客户，在首次超时或 10 倍阈值漏回复时，若原接待已过自己的班次时间，自动把客户转给当前当班且在线的同组客服，避免客户长时间没人回复。

## 验收清单

- [x] 只有同班次且在线的同组客服才会被选为目标。
- [x] 首次超时和 10 倍漏回复均触发自动转接判断。
- [x] 没有可接目标或转接失败时，继续原有提醒并@主管。
- [x] 不重置待回复计时，不改变原运营账号绩效归属。
- [x] 配置、业务策略和接口适配分层；补充单元测试与定向验证。
- [x] 值班颜色/组长规则已从选人逻辑中移除（2026-09-16 简化）。

## 落地记录（2026-09-16）

**根因**：原实现走 `POST /api/chat/assign`，该路径从未存在（2026-09-16 09:19 实测 HTTP 404）→ 自动转接从未成功。平台把“分配会话”做成 **socket.io 事件**：页面只有一条 `wss://<域名>/ws/socket.io/`，上面挂 `/client`（聊天与分配）与 `/notify`（只收通知）两个命名空间，事件帧形如 `42/client,["assignChat",...]`；已用只读探针实测捕获真实帧。

**链路要点**：

- 判定与挑人：`src/features/timeoutAutoTransfer/timeoutAutoTransferPolicy.js`（`decideTimeoutAutoTransfer`、`listOnShiftGroupMembers`、`pickOnlineTarget`）。
- 发指令：`attemptTimeoutAutoTransfer` → `42/client,["assignChat",{chatId,groupId,assigneeId}]`。
- 核验：`autoTransferVerificationStore.js` 登记待核验，`workflowRunner.settlePendingAutoTransfers` 按联系人快照确认 `assignedTo`。
- 通知：`autoTransferNotifier.js`，成功发群简报、失败/无人可接 @主管。
- 在线依据：上班监控每轮发布的共享快照（`onlinePresenceSnapshotStore.js`，TTL 90 秒，覆盖当天全部排早/晚班的人），转接策略只读快照，不重复读页面。

**验收（应有 vs 实有）**：

| 环节 | 应有 | 实有 |
|---|---|---|
| 自动化测试 | 全部通过 | 全绿（见仓库 `npm test` 当前基线） |
| 转接通道 | 发指令后平台真改派 | 黎经理→柯紫婷、→李守耀 均真实生效 |
| 在班判定 | 在班不转、不在班才转 | 11:15「一路有你」在班→跳过；13:18 李守耀在班→跳过 |
| 重叠期不误转 | 早班人在班就不转 | 15:00 售后早班在班→跳过（修复前会误转） |
| **自动转接全链路** | 提醒→判定→转接→核验→通知 | **2026-09-16 12:00:42 首例真实生效**：客户黎路遥，原接待柯紫婷（售后晚班，已过班次）→ 自动转给李守耀（当班在线），2 秒后核验通过（12:00:44），企微群已收成功通知 |

## 历史口径（已被上方最终口径取代，保留备查）

- 2026-09-16 上午：目标 = 同组 + 当天该班次 + **有值班背景色** + 在线；售前浅绿 `#E2F0D9`、售后浅蓝 `#BDD7EE`。
- 2026-09-16 中午：售后“组长在班即值班”（排班表不标色），组长优先；`targetDutySource=group_leader|background_color`。
- 2026-09-16 下午：确认黄色 `#FFFF00` 是“预留假期占位”，无值班含义；休息/年假/行政不参与。
- 2026-09-16 傍晚：用户指出“售后有别人在线就可以转，不必是值班人”，随后进一步简化为“同组 + 当班 + 在线”一条规则，删除全部值班/组长/颜色选人逻辑。
- 保留：`排班颜色清单` 诊断日志（`dailyScheduleService.js` 的 `summarizeDutyColors`），用于对账排班表颜色。

## 排班背景色的实测含义（2026-09-16 日志取证，仅诊断用）

| 日期 | 带色人员 | 颜色 |
|---|---|---|
| 09-16 | 叶炳辉(晚) / 刘秀文(早) | `#E2F0D9` 浅绿 |
| 09-16 | 柯紫婷(晚) | `#BDD7EE` 浅蓝 |
| 09-16 | 邓远祥(休息) | `#FFFF00` 黄（预留假期占位，无意义） |
| 09-17 | 韩欢欢(晚) / 徐佳楠(早) | 浅绿；缪婷婷(晚) 浅蓝；邓远祥(休息) 黄 |
| 09-15 | 韩欢欢(晚) / 徐佳楠(早) | 浅绿；陈燕玲(晚) 浅蓝 |

## 边界

- 不改变排班表内容，不把“排班早/晚”文字之外的东西当作值班依据。
- 排班表读不到、在线快照过期时**宁可不转**，也不猜目标。
- 任何**测试性**的真实改派仍需用户点名会话后才能做。
