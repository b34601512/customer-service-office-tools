# 配置编辑器可用性

## 意图、背景
分享给非技术主管后，全部配置应能看见并编辑。完整阅读发现：TUI 只渲染列表开头，选择到屏外看不到；文本再次编辑读旧值；关键词暂存为文本再次打开被当数组而清空；独立网页缺少三个 JS 必需开关，初始化直接报错。

## 验收
- [x] 选中字段及编辑提示保持可见；关键词列表同样滚动。
- [x] 再次编辑保留未保存草稿。
- [x] /settings 页面具备转接待开关和群聊识别控件，渲染和保存无缺失节点。

## 边界
只修输入输出，不改变业务配置规则或用户已保存内容。
