# 登录确认回跳与任务假运行

## 意图

用户完成真实登录后能进入监控；不能强行回跳旧地址、卡在保存登录态或需要退出整个控制台才能取消。用户要求进一步排查相关BUG后重新交付，接续009但不能将离线冒烟等同真实登录验收。

## 证据

- 2026-09-08 10:04:21 收到登录确认，loginFlow 未检查其他标签页，直接将原page导航到旧配置 zan-mh 的完整聊天地址。
- 10:04:22 被重定向到 oauth.youzan.com/login/captcha，10:04:23 抛登录失效。截图约10:05仍显示登录任务运行中/保存中。用户后来结束了该进程，采集时PID44964已不存在。
- Windows Node v24.19.0 最小子进程复现：readline.question 回车后 rl.close，设置 exitCode=1；2秒后仍有 PipeWrap且未退出；父进程关闭stdin后才退出。这不是浏览器启动速度问题。
- 代码已证实：仅检查最初page、未就绪先强跳旧URL、登录确认不释放管道、首次登录期间无停止入口、首次登录未收到确认也可被后台启动强杀接管、startTask在await依赖检查期间没有并发启动锁。
- 是否实际登录开了新标签页/不同域名，等待用户补充；不得把推测写成已确认事实。

## 实施边界与验收

- [x] 登录确认查找应用Context内真实聊天页，先验证再保存，不强行导航回旧URL；多个工作台不擅自选择账号。
- [x] 未完成登录时保留可见窗口并给出清晰提示，等待下一次明确确认；关闭窗口/输入结束能取消。
- [x] Windows真实子进程确认后能够自然退出；成功/失败都正确刷新控制台状态。
- [x] TUI所有任务可单独停止，停止中禁止再启任务；网页允许退出登录任务；去掉强杀登录接管逻辑，拒绝并发启动。
- [x] 定向测试后集中回归；不发通知、不改接单。
- [x] 更新干净分享包，保留旧包但明确不再使用；业务规则及用户数据不变。
- [x] 用户真实账号重新登录验收：10:35:45真实聊天工作台验证成功，10:35:46登录子进程自然结束，控制台API为login/idle，登录态valid。

依据：Playwright https://playwright.dev/docs/pages 明确Context包含多个Page/新标签页；Node https://nodejs.org/api/readline.html 与 https://nodejs.org/api/process.html 区分关闭readline、活动句柄和exitCode。管道问题有本机实测交叉验证。

## 验证记录（2026-09-08）

- 定向35/35；集中全量379/379（隔离目录 supervisor-tests-abgIje）；最终收口任务/管道/分享包定向14/14（supervisor-tests-sHm0Or）。因失败测试导致生产代码返工：0次；测试全通过后未反复全仓测试。
- 真实 Edge152.0.4191.66 本地伪工作台：新标签页识别、原页不回跳、DOM操作及持久化复开成功。
- 实际本地 /settings 与首页：无页面异常，编辑主管名保存成功；模拟登录进程更新地址后，旧表单保存未覆盖新地址。
- 包内 Node24.19.0 + 真实 Edge + 实际 TaskService/runLoginMode/loginFlow：一次确认后写入有效登录态和新标签地址，子进程自然exit0、任务idle，约6秒结束。入口网站是127.0.0.1伪页面，不等同真实账号验收。
- r2 ZIP：D:\桌面\客服督办分享包\客服督办-Edge-r2-20260908.zip；38,648,023字节；630条目；253份src文件与工作区逐份哈希一致；无runtime、Cookies、登录数据库、测试或Git资料；运行配置和企微目录为空；Node签名Valid。
- SHA256：3208DB60F8C538A4D32E6DE0B3811A1BF2E25F1DB5C1661EE929D4ABABACDBE9。
- 废弃的processExitWaiter.js已移至D:\备份文件夹\客服督办-20260908-r2，可完整恢复。旧分享包未删除，不应继续分发。
- ZIP检查最初把Playwright源码cookieStore.js误识别为Cookies数据库，已收紧为路径末段精确名称，复核通过；没有因此改动生产代码。
- 未启动线上监控，未发送企微通知，未改变接单开关，未读取或复制实际Cookie/token。

## 用户验收动作

2026-09-08用户报告登录生效但数据33分钟未更新：只读现场日志证明10:35:45人工登录验证成功，10:35:46子进程正常结束；/api/state实时返回taskName=login,status=idle,awaitingConfirmation=false,isValid=true。没有后台启动记录，数据停在10:03:12是尚未启动监控，不是新登录任务卡死。未代用户启动会发送通知/改变接单的后台任务。真实登录验收门槛已补齐。

自动续办核查1：上一工作轮有实质进展（修复、隔离验收与r2交付）；本轮只读确认没有匹配该项目的Node进程，现场日志仍截止10:06退出，登录态仍为10:04失效。没有可等待的在途登录任务，不能把此状态视为正在等待运行结果。真实账号登录需用户操作，此阻塞已连续出现2个目标轮次（含原交付轮），暂不标记goal blocked，也不额外启动监控。

自动续办核查2：前一轮属无进展而非已验证等待。本轮再次查询实际进程，仍无该项目Node进程；登录态仍为10:04:23 invalid。真实账号验收必须由用户先完成登录，没有可安全代办的登录或在途任务；同一阻塞已连续3个目标轮次，goal标记blocked（不是complete）。收到用户完成登录或新现场后恢复核验，不重复运行离线测试或启动线上通知。

退出旧控制台后重开原启动中心 → 首次登录 → 在Edge进入聊天工作台（若有多个组织只留需要监控的工作台） → 回1总览选择完成登录并继续 → 应在验证后结束登录任务且登录态有效。失败请保留窗口和日志，勿再压缩使用中的文件夹分享。
