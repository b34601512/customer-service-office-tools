# 断点记录（1.客服超时督办）

> 覆盖更新，只留最新一份。上限 ~40 行；历史查 `git log -- 断点记录.md` 或 `../../失败台账.md`。

## 一、当前状态（2026-09-18 11:00）

- **后台在跑**：PID **32940**（2026-09-18 10:04:11 启动，`node src/main.js run`，无窗口；重启前的现场另存 `runtime/current-run.log.before-20260918-restart`）。
- **已修复待闭环**：转接帧命名空间前缀（`/client,`）——旧写法"临时扫最近 40 帧猜前缀"会被心跳挤光 → 发哑弹。
  已改为"见过就永久记住"，认不出就**拒绝发送**并 @主管（`chat_namespace_unknown`），选 socket 优先命中聊天命名空间。
  **待做：下一例真实超时转接看是否打 `转接已核验` → 出现即关闭 issue #665**（不要私自用真实会话做联调，需用户点名）。
- 修复提交 `a46701d`；单测 **452/452**（含现场复现 + "一帧都不许发" 反向断言）。
- 口径（触发转接/目标/在班不转/交班补判）**唯一真源 = 本目录 `CLAUDE.md` + `README.md`**，别在这儿抄第二份。
- 上一轮验收记录（首例真实全链路 09-16 12:00:42 等）见 `../../验收清单.md`。

## 二、下次开工第一步（照做）

1. 后台在跑吗？
   ```powershell
   Get-CimInstance Win32_Process -Filter "Name='node.exe'" | Where-Object { $_.CommandLine -like '*src/main.js run*' } | Select-Object ProcessId,CreationDate
   ```
2. 转接健康度：
   ```bash
   cd "D:/桌面/办公软件/1.客服超时督办"
   grep -nE "已发送转接指令|转接已核验|转接未生效|发送转接失败通知|交班补判|主线:失败" runtime/current-run.log | tail -40
   ```
   「转接已核验」正常；「转接未生效」/「发送转接失败通知」= 群里已 @主管 → 查根因。
   夜里 22:30（售后）/ 23:45（售前）之后应完全静默。
3. 自检：`npm test`（**基线 452**，必须全绿）。

## 三、常用命令

```bash
# 启动（隐藏窗口）
powershell -NoProfile -Command "Start-Process -FilePath 'node' -ArgumentList 'src/main.js','run' -WorkingDirectory 'D:\桌面\办公软件\1.客服超时督办' -WindowStyle Hidden"
# 停止（先取 PID 再杀；项目自己的清理动作）
taskkill //PID <PID> //T //F
# 在线快照
grep "刷新在线状态" runtime/current-run.log | tail -3
# 状态文件
runtime/missed-reply-monitor/state.json   # 谁在等回复
runtime/auto-transfer-sweep/state.json    # 交班补判账本（只在真正动手时写）
```

## 四、踩坑（1号专属）

1. **要联调浏览器必须先停后台**（同一登录态/用户目录会被占用；停机约 2 分钟，状态持久化，重启后提醒照常触发）。
2. **转接的真实写操作**（改派客户）只在明确需要时做；测试性改派必须用户点名具体会话。
3. **1号 9371 端口是交行 U 盾驱动占用**，不是浏览器调试端口，无法 CDP 附加。
4. 别用 `node -e "require('./tests/...')"` 跑测试（会往真实 `runtime/` 写数据）；只用 `npm test`。
