# 断点记录 — 13号 客服培训课件制作

> 覆盖更新，只留最新一份。最近更新：2026-09-14（下午）
> 当前任务：按领导要求，给京东6店（自营）的指定客户逐个做培训课件（客户号由用户提供）。

## 交付状态

| 客户 | 场景 | 状态 | 产物 |
|---|---|---|---|
| jd_5d2092e386b8e | 议价 | ✅ 用户已确认「没问题」 | `runtime/outputs/2026年8月/议价应对案例演示-20260821.html`（自检 10/10） |
| jd_4e7ead55726dd | 基础答疑（客户连问 3 个基础问题，客服没答到位） | ⏳ 已出片，等用户看 | `runtime/outputs/2026年8月/基础答疑案例演示-20260824.html`（自检 10/10） |

两个客户的解析文件都在 `runtime/review/`（同基名）。改内容 = 改解析文件重跑 `generate`，**不要手改 HTML**。

## 待用户拍板

1. `jd_4e7ead55726dd` 课件是否可用 / 要改哪：建议话术里「京东自营发货、全国联保」「Q5L/Q5S/Q5W 三款」等表述是否符合店铺现行口径。
2. 是否还有下一个客户要做。

## 模板头已砍掉（2026-09-14 用户要求）

- 用户要求：不要顶部那个红色的吓人摘要卡，课件打开**直接进正文**（会话回放），标题只放浏览器标签页。
- 已改：`renderCourseware.js` 删除 `.header`（tagline/大标题/案例看点/使用提示）与对应 CSS；会话信息条去红（灰底灰字，保留日期、客户ID 眼睛按钮）。
- 已加程序铁律：`selfCheck` 新增「无顶部大标题/摘要卡（打开即正文）」，出现 `class="header"` 直接自检 fail（现在共 11 项）。
- 已改经验记录（避免后续 AI 再踩坑）：`SKILL.md` 铁律 7、`README.md` 铁律 8、`AGENTS.md` 铁律括注、`历史资料备份/经验模板-聊天记录演示制作方法.md`（顶部加「最新约定」+ 把 `.header` 标为已废弃）。
- 两个课件已按新模板重出，自检 11/11 全绿。

## 数据事实（备查）

- 京东6店 = 后台主账号 **DEDAKJ自营--小黛**，客服管家页头显示「自营店铺模式DEDAKJ京东自营旗舰店」。
- `jd_5d2092e386b8e`：近18个月仅 2 个会话，都在 2026-08-21 —— `_咨询`（20 条，Q5 三款选型）与 `_议价`（4 条，问优惠 → 客服"低价/活动价" → 39 分钟后才补晒单送 3 套过滤）。
- `jd_4e7ead55726dd`：近18个月仅 1 个会话，2026-08-24 08:37-08:39（6 条）—— 客户问「要加制氧剂吗 / 是不是加纯净水 / 什么牌子」，客服依次回：表情、「亲看的哪款呢」、「德迩杰」，客户离开后再没进线。
- ⚠ 京东咚咚表情在聊天记录里存成编码（如 `#E-s33`）；已在转换层统一转成「（表情）」，别再当乱码处理。

## 常用命令

```bash
cd "D:\桌面\办公软件\13.客服培训课件制作"

# 浏览器（9223 端口，资料目录 runtime/browser-profile-jd6，登录态已落盘；失效时重跑并人工登录）
node -e "const p=require('path');const{launchVisibleBrowser,waitForDebugPort}=require('./src/services/browserSession');(async()=>{const port=9223;const userDataDir=p.join(process.cwd(),'runtime','browser-profile-jd6');const targetUrl='https://shop.jd.com/jdm/kefu/kf-manage-lite/#/UtilsSetting/ChatLog';try{const r=await launchVisibleBrowser({browser:'chrome',port,userDataDir,targetUrl});console.log('pid='+r.pid)}catch(e){console.log(e.message)}console.log('端口就绪:'+await waitForDebugPort(port,{timeoutMs:10000}))})()"

# 按客户号查/存会话（18 个月上限；--start/--end 可先宽后窄，保存建议用单日窗口）
node src/cli.js fetch:list --start 2025-04-01 --end 2026-09-14 --customer <jd_客户号> --port 9223
node src/cli.js fetch:save <sid> --start 2026-08-24 --end 2026-08-24 --customer <jd_客户号> --port 9223

# 出片 + 自检（必须全绿）
node src/cli.js generate <聊天文件基名>

# 回归测试（Node 24 下不要写 node --test tests/）
node --test
```

## 源码修复记录

- `997f3b1`（已推送）：jdConvert 新版消息包按 sid 聚合；jdFetch 页面识别（shop.jd.com/jdm/kefu/*）/NO_PROXY/宽窗口按会话真实日期；cli 自检打印；SKILL 测试命令。
- 本次新增（已推送）：`jdConvert.normalizeEmojiCodes` —— 京东表情编码 `#E-sXX` → 「（表情）」。

## 注意

- `runtime/`（聊天记录、解析、成品 HTML、浏览器资料目录）已 gitignore，不入库。
- 课件铁律：就地 `details.insight`、无目录/结尾总结卡、去客服姓名、客户ID 默认脱敏、消息顺序=真实顺序。
