# 20.经验大全

一个经验一个文件夹，供人和 AI 直接复用：每个文件夹内有一份经验说明 Markdown；需要脚本的经验再附一份可直接运行的只读脚本。

## 目录

| 文件夹 | 经验 | 配套脚本 |
| --- | --- | --- |
| [金山在线Word文档读取](./金山在线Word文档读取/) | 金山在线 Word/大纲文档（`otl`）无 GUI 读取正文与图片 | `金山在线Word文档读取.cjs`、`下载文档图片.cjs` |
| [金山在线表格排班表读取](./金山在线表格排班表读取/) | 金山在线表格（排班表）读取值矩阵与单元格有效背景色（条件格式） | `金山在线表格排班表读取.cjs` |
| [pi上下文20万自动压缩方案](./pi上下文20万自动压缩方案/) | pi 上下文 20 万自动压缩：空闲触发不打断 run + 摘要输出上限自愈（迁移话术，v3） | `冒烟测试-auto-compact.cjs` |
| [Windows进程与占用排查](./Windows进程与占用排查/) | 目录被占用 / 端口被占 / 该杀哪个 node、python 进程 | `查进程.ps1`、`结束进程.ps1` |
| [密码墙网页读取](./密码墙网页读取/) | JS 渲染 + 密码墙（加密文档/飞书 wiki/探域后台等）用本机可见 Edge + 持久画像读取；含输密码、四件套验收、Cookie 页面内取数 | `密码墙网页读取.cjs` |
| [crawl4ai网页采集](./crawl4ai网页采集/) | 把网页变成 LLM 可用的干净 Markdown，免掉爬虫服务月费（自托管开源）；含原帖说法核实清单 | 暂无 |

## 使用约定

- 先读对应文件夹里的 `*经验.md`，里面有完整步骤、实测结论和常见坑。
- 有配套脚本时优先直接运行，不要重写。脚本依赖借用已有项目即可：

```powershell
$env:NODE_PATH = "D:\桌面\办公软件\1.客服超时督办\node_modules"
node "<脚本路径>" --help
```

- 所有脚本默认只读：不写入在线文档，不保存、不打印 Cookie/token，真实登录态只留在本机浏览器画像。
- 密码类经验统一约定：密码只从环境变量或本机文件读入，示例一律用 `<密码>` 占位，不入库、不进聊天记录。

## 新增经验

1. 新建文件夹，文件夹名就是经验名；
2. 放入 `<经验名>.md`（关键代码可以直接内嵌在 Markdown 里）；
3. 如需可复用脚本，放入 `<经验名>.cjs`，并在 Markdown 开头注明“配套脚本：同目录 …”；
4. 更新本 README 的目录表。
