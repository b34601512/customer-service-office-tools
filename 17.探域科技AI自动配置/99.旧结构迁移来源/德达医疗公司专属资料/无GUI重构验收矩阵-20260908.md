# 17号项目执行方式与API重构验收矩阵

> 当前规则：禁止有头GUI；允许直接API，或使用 `headless:true` 进行无头GUI测试/承载登录态调用API。页面控件操作仅限测试，不作为后台配置正式入口。核心脚本意图见 `scripts/intent-index-20260908.json`。

## 已完成

| 功能 | 当前唯一入口 | 证据 | 状态 |
|---|---|---|---|
| 自定义Agent | `custom-agent-api-20260907.cjs` | 列表/详情读取；同文草稿保存、回读、SHA核验 | 已验证 |
| Agent风控审计 | `audit-custom-agent-risk-20260908.cjs` | 当前“售前咨询”正文读取成功并命中3类高风险表达 | 已验证 |
| 接待配置读写 | `agent-config-api-read-20260908.cjs`、`reception-config-api-20260908.cjs` | 当前15天、策略1；同文保存后回读一致 | 已验证 |
| 自动发送读写 | `auto-send-api-20260908.cjs` | 同文写入回读业务字段一致；系统重建了planId | 已验证（勿重复测试） |
| 转交前话术读写 | `before-transfer-api-20260908.cjs` | 同文保存回读通过，当前话术0条 | 已验证 |
| 模拟器只读/测试 | `simulation-api-read-20260908.cjs`、`simulate-blood-oxygen-recommend-20260907.cjs` | API读取10条上下文/10张卡片；测试前后重置；两题均独立收到新回复 | 已验证 |
| 有头GUI入口清理 | 项目脚本目录 | 71个有头GUI脚本已移出；仅保留1个已验证无头测试入口 | 已验证 |

## 已还原但未执行写入

本地待确认草案：`待确认-Agent售前缺氧承接草案-20260908.md`。仅供业务评审，尚未写入后台。
草案本地验收：`validate-sales-draft-20260908.cjs` 已通过6项检查，包括无正向血氧/档位参数映射。

| 功能 | 已核对写入结构 | 未完成原因 |
|---|---|---|
| 接待订单判定 | `orgId + configs[agent.reception.salesStageJudgment]` | 已完成同文保存回读；后续勿重复测试 |
| 自动发送 | `{plans: [...]}` | 已完成一次验证；后续勿重复写入，避免重建planId |
| 转交前话术 | `{beforeTransferSpeeches: [...]}` | 已完成一次同文验证；当前为空，不新增内容 |
| 智能触发器 | `name/priority/ifAnd/filters/triggers/transferActionDto/...` | 当前无规则，不得自行新增 |
| 意图配置 | 咨询/回复配置数组 | 当前为默认空配置，不得自行新增 |

## 明确禁止

- 不按疾病、单次血氧或医院档位直接推导家用氧疗流量、浓度、时长。
- 不发布未经用户确认的Agent正文、自动发送策略、触发器或意图规则。
- 不使用备份目录中的有头GUI脚本作为操作入口。
- 模拟发送、重置会话、trace属于外部行为；无头血氧脚本仅用于隔离测试，不能代替正式配置回读。

## 后续执行顺序

1. 用户确认后，逐项执行“当前值同文写入→回读→哈希核验”。
2. 用户确认业务规则正文后，再更新草稿并单独确认发布。
3. 取得安全测试文本后，接入隔离模拟发送和trace回读。
