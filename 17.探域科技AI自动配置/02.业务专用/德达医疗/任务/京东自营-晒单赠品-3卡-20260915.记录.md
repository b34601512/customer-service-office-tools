# 京东自营·晒单赠品 3 张话术卡（新建·停用）— 2026-09-15

## 为什么做

用户 2026-09-15 指示：查这 3 类话术（来自 13 号项目客户案例 `jd_51164c4a52cb5` 血氧仪赠品）后台有没有，没有就做到后台；**做好后不要启用，因为京东自营店还没接入探域**。

## 依据（写入前已核对）

- 京东自营店活动图「购机尊享福利」：**下单即送**吸氧常备礼包（吸氧管 2米×1、吸氧面罩 2米×1）；**晒图加送**血氧仪＋额温枪。
- 业务口径（用户 2026-09-15 确认）：晒单礼需**确认收货后**晒图评价才登记；客户一纠结就**直接登记补发**、不再要求晒单（内部不需主管审批，对外只讲规则和结果）；礼品由**湖南总工厂**发出、一般 **1-2 个工作日**、默认**邮政或韵达**。
- 案例台词来源：13 号项目 `runtime/chat/2026-08-29_jd_51164c4a52cb5_血氧仪A.chat.json`、`2026-09-01_..._血氧仪-0901.chat.json`。

## 操作流水（可复现）

```powershell
# 1) 只读盘点（确认后台没有京东自营卡：京东 0 张、额温枪 0 张）
#    证据：D:/备份文件夹/探域问答审核-20260915/inventory-summary-20260915.json
# 2) dry-run
node .\01.通用\工具\探域知识卡任务执行器.cjs --task ".\02.业务专用\德达医疗\任务\京东自营-晒单赠品-3卡-20260915.json" `
  --base-url http://agent.tanyuai.com --profile "C:/Users/b3460/.pi-edge-auto" --browser-channel msedge `
  --backup-dir "D:/备份文件夹/探域问答审核-20260915" --action check          # → ready: 3（无同名冲突）
# 3) 写入（新建，默认停用）
... --action apply --allow-write true                                        # → created-awaiting-index: 3
# 4) 索引刷新后回读
... --action verify                                                          # → already-target: 3
# 5) 字段核验（只读脚本 verify-jd-cards.cjs）：ifOpen=false、绑定店铺为空、订单阶段正确
```

## 结果对账（应有 vs 实有）

| 指标 | 写入前 | 写入后 | 应有 | 结论 |
|---|---|---|---|---|
| 全库卡数 | 2047 | 2050 | +3 | ✅ |
| 启用卡数 | 2012 | 2012 | 不变（不启用） | ✅ |
| 停用卡数 | 35 | 38 | +3 | ✅ |

## 新建的 3 张卡（当前全部**停用**、**未绑定店铺**）

| # | 卡片 ID | 标题 | 订单阶段 | 状态 |
|---|---|---|---|---|
| 1 | `6aa8ff58729b323bc44093ca` | 【京东自营·待启用】晒单赠品/1.有送血氧仪吗？（第一次就要说清） | PRE_SALE | 停用 |
| 2 | `6aa8ff71e4bec547547eb13a` | 【京东自营·待启用】晒单赠品/2.为什么要晒单？不想晒单？（直接登记补发） | AFTER_SALE | 停用 |
| 3 | `6aa8ff89729b323bc44093d9` | 【京东自营·待启用】晒单赠品/3.血氧仪发货了没有？（催进度） | SALE + AFTER_SALE | 停用 |

## 启用清单（等京东自营店接入探域后再做）

1. **绑店**：把 3 张卡的 `includeCondition.shop` 设为京东自营店的 `thirdShopId`（现在是空数组，故意留空：本账号下只有拼多多店，不把京东话术挂到拼多多范围）。
2. **复核活动**：确认「晒图加送 血氧仪＋额温枪」仍在有效期内、且与当时宣传图一致（按"宣传是什么就是什么"口径）。
3. **打开 `ifOpen`**（逐张），然后用无头模拟测试跑买家用例，最后回读核验；未核验前不得对外说"已生效"。
4. 本账号**拼多多店**的既有晒单/赠品卡一律不动（本次启用数 2012 未变化可证）。

## 证据文件（本机私有备份，不入库）

`D:/备份文件夹/探域问答审核-20260915/`

- `inventory-summary-20260915.json`、`cards-page-20260915.json`、`cards-keyword-hits-20260915.json`（写入前全库快照与关键词命中）
- `cards-page-after-20260915.json`、`verify-jd-cards-20260915.json`（写入后快照与字段核验）
- `knowledge-task-2026-09-15T08-18-13-692Z/`（check）、`knowledge-task-2026-09-15T08-18-26-292Z/`（apply：task、payload、journal）、`knowledge-task-2026-09-15T08-21-58-436Z/`（verify）

## 工具改动

`01.通用/工具/探域知识卡任务执行器.cjs` 增加**新建**支持（原来只支持原位更新）：

- 任务项不含 `id`、含 `title` 时按新建处理；新建默认 `ifOpen:false`（**必须显式写 `business.ifOpen:true` 才启用**）。
- 同标题已存在且内容一致 → `already-target`；已存在但内容不同 → `title-conflict`（不覆盖、不重复建，交调用方决定）。
- 写入后按返回 id / 标题回读，核验正文 + 停用状态；索引未刷新时报 `created-awaiting-index`（等下一次 verify）。
