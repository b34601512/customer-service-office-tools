# 断点记录（多项目自动化执行任务）

> 覆盖更新，只保留最新一份。位置：`D:\桌面\办公软件\断点记录.md`
> 最近更新：**2026-09-15 11:05** ｜ 状态：**P1~P6 全部完成（P4 已补齐 9/9 家店）**；仅 P6 的企微反馈等你确认是否发送

## 一、任务来源

用户要求按顺序自动化执行 6 个项目（P1~P6），逐项验收并汇报；遇到必须人工的登录/滑块要停下来叫人；业务口径不猜。
本次执行清单：`验收清单.md`；本轮复盘与提示词模板：`自动化执行经验.md`（**下次开跑前先读这两份**）。

## 二、各阶段状态（全部完成）

| 阶段 | 状态 | 关键事实 / 产物 |
|---|---|---|
| P1 `1.客服超时督办` | ✅ 通过 | `node src/main.js run` 后台启动（等价 TUI「后台启动」，真源 `src/controlCenter/taskService/taskConfig.js:18`），**PID 25816**（08:35:27 启动，`runtime/supervisor.pid`）。终检 **新鲜度 95%**（阈值 120s，客户 89）；今天无新超时/漏回复提醒（最近一次提醒 09-04） |
| P2 `2.发票自动化`（5 子项目） | ✅ 完成（只读，未提交/未回传） | 京东开票巡检 5/5 店成功、识别 0 条；京东催票 11 条=processing 4/handled 7、**无待处理**；天猫 **9 单待处理**（7 可录入+2 待同意，其中 3 单已超 7 个工作日）；拼多多 0；抖音 **1 单待处理**（¥2399）。**合计 10 单待处理** |
| P3 `5.电话漏接分析` | ✅ 完成 | 菜单7 下载成功（记录 `20260915_084650_ffcab91e`，报表范围 2026-06-18~09-15）。只看今天+昨天：呼损共 **9** 条＝排队 **1** 条＋IVR **8** 条；**需回访 0 个**（唯一排队呼损客户 10 分钟后已成功呼入并通话 186 秒）。脚本 `scripts/query_queue_loss.py`，报告 `runtime/queue_loss_report_20260915.txt` |
| P4 `9.客服数据自动更新` | ✅ 完成（9/9） | 本地「数据明细」**90 行 = 9 家店 × 10 行**，日期 **2026-09-01~2026-09-13**（本月1号~今天-2天），合计销售额 **597,736.19**；金山在线 **90 = 本地 90**；透视筛选 **2026-09-13，22/22 成功** |
| P5 `12.店铺指标数据自动更新` | ✅ 完成（9/9） | 9 家店全部成功（含补跑京东3店），金山一键同步数据源 **在线 231 = 本地 231**（12 列）；数据源 `outputs/019fbb96-c39c-7ec1-899b-038594c1381a/店铺指标数据源.xlsx`（09-15 09:55） |
| P6 `19.聊天质检` | ✅ 报告已出 | 取 2026-09-15 售前会话 **12 条**（德达官方旗舰店，星星→徐佳楠 8 条、安安→叶炳辉 4 条），逐条落盘 `runtime/chat/2026-09-15_*.chat.json`；报告 `runtime/outputs/质检报告-2026-09-15-售前.md`。结论：响应 5–20 秒、答疑准确，**主动跟单明显不足**；问题＝漏回复 1 条、只回「在的」悬空 2 条、「稍等」无下文 1 条、答复笼统 2 条。**企微未发送** |

## 三、本次修过的代码（已提交并推送）

| 提交 | 内容 |
|---|---|
| `7ed6f54` | **9号天猫登录竞态不再让整店失败**：`tmallLoginSurface.js` 新增 `isTmallLoginSurfaceRaceError`，`findFirstVisibleLocator` 遇上下文中断/元素脱离返回 null；`tmallLoginAutofill.js` 的 `hasVisiblePasswordInput`/`switchToPasswordLogin`/`fillTmallLoginFrame` 竞态一律收敛为 false，真实错误照抛；测试 `tests/tmallLoginRaceTolerance.test.js` |
| `f542150` | **9号下载等待补墙钟上限**（治"静默卡死"）：`downloadEventEngine.waitForDownloadArtifactState` 的名义 60s 超时基于 `getAutomationTime()`（= `Date.now()-humanWaitMs`），人工守卫反复耗时会把它无限推迟 → 新增墙钟上限＝下载时限+`humanTimeoutMs`，到点返回 null 让失败可见；测试 `tests/downloadWaitWallClockCap.test.js` |
| `f542150` | **12号弹窗点击期间弹层自行消失不再判失败**：`blockingPopupEngine` 新增 `clickExplicitCloseTarget`（点击超时但弹层已不在页面 → 按已关闭继续）；并加有界保护（连续 3 次"确认已消失却仍检测到遮挡层"就停下报错）；测试 `tests/blockingPopupSelfDismiss.test.js`（全量 151/151 通过） |
| `f542150` | 文档：新增 `自动化执行经验.md`；`AGENTS.md` 增补「自动化执行铁律」 |

## 四、本次新增可复用脚本

```bash
# 9号：全部汇总 + 金山一键同步明细 + 设置透视筛选日期（含启动日期重算，缺了它数据不是最新）
cd "9.客服数据自动更新" && node scripts/runSummaryAndKdocsSync.js

# 9号：只补指定店铺（不会清空其他店铺数据）；任务ID 形如 pdd-pdd03-all，见 scripts/retryFailedStores.js
cd "9.客服数据自动更新" && node scripts/retryFailedStores.js jd-jd1-all pdd-pdd03-all douyin-douyin2-all

# 9号：抖音登录态失效时想让它开可见窗口等人工短信验证（默认 hybri 先无头后切可见）
cd "9.客服数据自动更新" && CUSTOMER_PERFORMANCE_BROWSER_MODE=headed node scripts/retryFailedStores.js douyin-douyin2-all

# 12号：全部汇总 + 金山一键同步数据源（当天已成功的店会自动跳过，适合补跑失败店）
cd "12.店铺指标数据自动更新" && node scripts/runSummaryAndKdocsSync.js

# 5号：今天+昨天的排队呼损只读查询（菜单1呼入+菜单3呼损口径）
cd "5.电话漏接分析/电话漏接分析后台" && python scripts/query_queue_loss.py

# P1 新鲜度自检（客户页同一算法）
cd "1.客服超时督办" && node -e "const{readMissedReplyMonitorState}=require('./src/features/missedReplyMonitor/missedReplyStateStore');const{buildCustomerMirrorItems}=require('./src/controlCenter/customerMirrorListBuilder');const{buildDataFreshness}=require('./src/controlCenter/controlCenterDashboardService');const n=Date.now();const st=readMissedReplyMonitorState();const it=buildCustomerMirrorItems(st,n);const f=buildDataFreshness(it,n);console.log('新鲜度='+Math.round(Math.max(0,Math.min(1,1-f.ageSeconds/120))*100)+'% 年龄='+f.ageSeconds+'s 客户='+it.length)"
```

## 五、失败清单与处置（本次实跑，供下次参考）

| 阶段 | 首轮失败 | 原因 | 处置 |
|---|---|---|---|
| P4 | 天猫2店 | 登录面执行上下文被销毁（`tmallLoginAutofill.js` 竞态）→ 整店失败 | **修代码**（`7ed6f54`）后重跑一次通过 |
| P4 | 京东1店 / 京东6店 | `Target page, context or browser has been closed`（下载等待超时被 finally 掩盖） | 重跑成功 |
| P4 | 拼多多02 / 拼多多03 | 点击下载表单后 60s 未落盘（只留 `.crdownload` 残片，09-12/09-14 也有同类残片） | 重跑成功 |
| P4 | 德达抖音 | 首轮没跑到（前面卡死被掐） | 重跑成功 |
| P4 | dedakj抖音 | 首次跑时登录态被判失效、且在登录恢复流程里点店铺菜单超时 | **单独重跑即恢复**（登录态其实有效，自动切店成功）；该店**无配置账号密码**，真失效时只能人工短信登录 |
| P5 | 京东3店 | 弹窗关闭按钮点击超时（元素不稳定） | **修代码**（`f542150`）后重跑成功（其余 8 店自动跳过） |
| P4 首轮 | 整轮 | 拼多多03 下载等待**静默卡死 43 分钟**、无任何输出，被监控无输出超时掐掉 | **修代码**（`f542150` 墙钟上限）；重跑补齐 |

## 六、待你拍板

1. **P2 发票**：天猫 9 单（7「可录入发票」+2「待同意」）+ 抖音 1 单 = **10 单待处理**。
   - 需要的话：天猫「待同意」2 单需在天猫后台点同意；其余在 TUI 里逐单按 `a` 标记「已安排客服跟进」。
   - 本次**只读巡检，未做任何平台操作**（不提交、不回传）。
2. **P6 企微反馈**：4 条候选稿已写好（见汇报正文/报告文件），**未发送**。要发需你确认文案 + 口令 `161323`。
3. **是否把「全天无促单收尾」当共性问题向售前组统一提醒**（而不是只针对个人）。
4. **`自动化执行经验.md` 是否也放一份到 `D:\SoftTalk\AGENTS.md`**（你给了这个路径，但它属于另一个仓库；我没擅自改，等你确认）。
5. **P3 IVR 呼损**：今天+昨天 8 条 IVR 阶段呼损，按你之前的口径「只看排队呼损」不计；如需回访 IVR 客户请说一声。
6. **天猫2店登录态**：本次是程序自动填账号密码后登录成功的（无验证码）；**天猫店铺8（2号发票项目）仍需人工验证**，与本次无关。

## 七、注意事项（踩坑）

- **端口冲突**：9号与19号都用 **9333**（必须串行）；12号用 9334；1号用 9371。跑 9号 采集前先关掉 19号 的 Edge。
- 关掉 19号 Edge 不影响其登录态（存在 `runtime/browser-profile`）。
- 9号/12号 汇总前必须确认目标 Excel 未被 WPS/Excel 占用（可用 `openSync(path,'r+')` 试写判断）。
- 9号 补跑单店**不会清空**其他店铺数据；12号 重跑会自动跳过当天已成功的店。
- 9号 douyin2（dedakj抖音）**没有配置账号密码**，登录态失效时只能人工短信登录（开可见窗口等，`DOUYIN_LOGIN_RECOVERY_TIMEOUT_MS` = 10 分钟）。
- 抖音/天猫/拼多多的 `.crdownload` 残片是下载未完成的历史痕迹，不要当成有效源文件（代码只认稳定 `.xls/.xlsx`）。
