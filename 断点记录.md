# 断点记录（多项目自动化）

> **上限 ~40 行**：只写「下次开工第一步 / 各项目状态一览 / 等你拍板 / 不要做的事 / 踩坑」。
> 历史过程**不写这里**（查 `git log -- 断点记录.md`、`失败台账.md`、`验收清单.md`）；细节写进**各项目目录**的 `断点记录.md`。
> 更新时间：2026-09-18 14:30

## 一、下次开工第一步

1. 读这份（40 行够了）；要动哪个项目，再读那个项目目录的 `断点记录.md`。
2. 检查 1号后台在不在跑（命令见 `1.客服超时督办/断点记录.md`），不在就按那儿的方式启动。
3. 看 1号 转接健康度；夜里 22:30（售后）/23:45（售前）后应完全静默。
4. 用 `/loop` 或 `/goal` 托管本次任务；收工覆盖更新**本项目**的断点记录（本文件只更新下面那张表）。

## 二、各项目状态一览（2026-09-18）

| 项目 | 当前状态 | 细节真源 |
|---|---|---|
| 1号 客服超时督办 | 后台 **PID 32940** 在跑（10:04 启动，晚于修复提交 ⇒ `/client,` 命名空间修复**已生效**，测试 452/452）；issue #665 已关 | `1.客服超时督办/断点记录.md` |
| 2号 发票自动化 | 只读巡检正常；**14 单待处理**（天猫 11 / 抖音 2 / 拼多多 1），其中超 7 工作日 5 单 | `2.发票自动化/CLAUDE.md` |
| 5号 电话漏接分析 | 窗口内呼损 8 条 = 排队 0 / IVR 8；**需回访 0** | 项目 README |
| 9号 客服数据自动更新 | 9 店 90 行（09-01~09-16），总售前业绩 **715,481.21**；金山 90=90、透视 22/22 | 项目 README |
| 12号 店铺指标数据 | 9/9 店成功、金山 **236=236**；弹窗可见性口径 bug 已修（`d3c5d5f`） | `12.店铺指标数据自动更新/断点记录.md` |
| 14号 后台工单提醒 | **常驻监控在跑**（菜单 [1]，一店一窗 9411 起、间隔 10 分钟、10 分钟新鲜度血条；生效店铺 jdtest + 京喜2店，jd1 enabled=false）；测试 86/86（`7578a90`） | `14.后台工单自动提醒/断点记录.md` |
| 抖音店铺5 下载 | 三处加固后真跑仍失败 → 等下次现场（`6.抖音发票回传/runtime/douyin-download-failures.log`）再定方案，不再盲改 | `失败台账.md` |

## 三、等你拍板

- 暂无（14号 京东1店 已定不跑；重启 bat 看新界面这件事已确认：你的实例 11:56 启动，晚于血条提交 11:40，自带新界面）。
- **issues 抽屉已清空（09-18，23 → 0）**；两个真风险留在关闭评论里：`#633`（9号 清空数据明细前无备份）、`#619`（历史敏感信息未全量扫描）——要动就重开新单。

## 四、不要做的事（已定，别再问）

- 天猫发票「待同意」单：**绝不自动点同意**（客服先手工登记才点，抢点会导致漏处理）→ 自动化只做只读巡检。
- 失败后：**不自动重试、不无脑补跑**；先记录现场 → 攒到规律（同一现象 ≥2 次）→ 根因修复 + 反向断言测试。
- 截图能力：**已全部删除，不要再加回**（含开关、半拆、空目录）。
- 京东账号目录统一：不做（2号用 Edge、9号用 Chrome，各登各的）。
- 「经验贴到 `D:\SoftTalk\AGENTS.md`」：不做（项目无关）。
- 打包脚本 / 分发包：已删，不再做。

## 五、踩坑（跨项目，照做）

1. **9号与19号都用 9333 端口，必须串行**；12号 9334；1号 9371 被交行 U 盾占用（不是调试端口）。
2. **每个会等人的步骤都要有"无输出上限"**：单店/单步骤超过 ~5 分钟无新日志就按卡住处理，停下来查（不许干等）。
3. **验收只认真实产物**（应有值 vs 实有值），不认脚本自报成功；修完必须重跑验证。
4. **需要人工验证（滑块/验证码/短信/扫码）停下来叫人**；任何真实提交/发送（发票回传、企微发送）先问用户。
5. **本仓库常有平行会话**：动手前 `git status`，改完及早提交；**提交只点名加自己改的文件，禁止 `git add -A`**。
6. **汇报规则**：只在需要用户处理/拍板时汇报；一次只问一个问题；30 字内说核心。
