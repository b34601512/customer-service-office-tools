# 断点记录（多项目自动化执行任务）

> 覆盖更新，只保留最新一份。位置：`D:\桌面\办公软件\断点记录.md`
> 最近更新：2026-09-14 11:35 ｜ 状态：**P1~P6 全部完成；新增一问「抖音发票识别日期为何不是今天」已查明原因，等用户拍板是否修（见「五之二」）**

## 一、任务来源

用户要求按顺序自动化执行 6 个项目（P1~P6），逐项验收并汇报；不要用后台监视隐藏执行；
遇到必须人工的登录/滑块要停下来叫人；业务口径不猜。

## 二、各阶段状态（全部已完成）

| 阶段 | 状态 | 关键事实 / 产物 |
|---|---|---|
| P1 1.客服超时督办 | ✅ 通过 | `node src/main.js run`（= TUI「后台启动」，真源 `src/controlCenter/taskService/taskConfig.js:18`），PID **8588**（08:37 启动）。终检新鲜度 **98%**（验收线 90%，阈值 120s）；客户判定 94；待回复候选 2 条（都是 10–14 天前已提醒过的历史遗留，已提醒不重复）。用户确认不改 TUI。 |
| P2 2.发票自动化 | ✅ 已汇报（待用户定是否推进） | 只读巡检。**关键结论：天猫 7 单全部是「待处理（未安排）」**（工具里的客服跟进状态字段：pending=待处理/processing=已安排客服跟进/发票已登记/已处理）；其他平台已安排：京东开票巡检 4 单=已安排、京东催票 4 单=已安排+7 单已处理、抖音 2 单=已安排、拼多多 0 单。 |
| P3 5.电话漏接分析 | ✅ 已完成（口径已按用户要求收窄） | 下载 2026-06-17~09-14；**用户要求只看昨天（09-13）+ 排队阶段** → 实测昨天 7 条呼损**全部是 IVR 阶段**（排队停留 0 秒），**排队阶段 0 条**（最近一次排队阶段呼损是 09-08）。7 个号码已列出，等用户确认口径（严格排队阶段 vs 含 IVR）。 |
| P4 9.客服数据自动更新 | ✅ 完成（已修正） | 首轮 6 成功/3 失败（京东店登录问题），修 2 个京东缺陷后补齐。**用户随后指出“金山没有最新数据”**：根因是**我直调汇总函数时跳过了 TUI/CLI 启动才做的日期重算** → 沿用了配置里的旧范围 2026-09-01~09-10，而智能模式应为「本月1号~今天-2天」= **09-01~09-12**。已在直调脚本补上 `initializeProjectConfigForStartup()`，重跑后：本地 `数据明细` **90 行 = 9 家店齐**（天猫2+京东3+拼多多2+抖音2），全部 **2026-09-01~09-12**；金山 **在线 90 = 本地 90**；透视筛选日期 **2026-09-12**，**22/22 成功**。另外修了抖音广告弹窗拦截切店点击的缺陷（见下）。 |
| P5 12.店铺指标数据自动更新 | ✅ 完成 | 汇总 **9/9 家成功**，金山一键同步数据源 **在线 232 行 = 本地 232 行逐格一致**，耗时 221s。数据源：`outputs/019fbb96-.../店铺指标数据源.xlsx`。 |
| P6 19.聊天质检 | ✅ 报告已出 | 取 2026-09-14 售前会话 12 条（京东客服管家，独立 Edge 端口 9333），按 sid 分段落盘 `runtime/chat/2026-09-14_*__<sid前8位>.chat.json`；AI 已逐条通读并出报告。**企微未发送**（需用户确认文案 + 口令 `161323`）。 |

## 三、本次修过的代码（已提交并推送）

| 提交 | 内容 |
|---|---|
| `4be57bf` | 京东登录页/安全验证不再被误判为遮挡弹窗（`hasJdLoginOrVerificationText`；`stabilizeJdBrowser` 跳过登录/验证面；人工验证正则补「拖动…拼图/箭头」） |
| `32544d8` | 京东登录填表竞态不再让整店失败（fill 限时 5s；「页面已跳走/元素脱离/上下文销毁」识别为竞态收敛返回 false，不吞真实错误） |
| `ea77ca8` | 新增 3 个非交互直调脚本（见下） |
| `90e1e0c` | **直调脚本补上启动日期初始化**（修「数据不是最新」根因）；**抖音广告弹窗拦截切店点击**：新增 `douyinAdPopupDismiss.js`，只在 clicks 报「intercepts pointer events」时定向关广告弹层（唯一明确关闭入口，否则 ESC 收起，绝不点促销 CTA），关不掉就报错停下 |
| `bbe2340` | 断点记录 + 发票只读同步/巡检脚本 |

## 四、新增可复用脚本

```bash
# 9号：全店汇总 + 金山同步 + 透视筛选日期（含启动日期重算——缺了它数据就不是最新）
cd "9.客服数据自动更新" && node scripts/runSummaryAndKdocsSync.js

# 9号：只补指定店铺（缺省=京东6店/拼多多03/德达抖音），同样含启动日期重算+金山同步
cd "9.客服数据自动更新" && node scripts/retryFailedStores.js [任务ID ...]
#      任务ID 见 scripts/runSummaryAndKdocsSync.js 输出或 TUI 店铺页；如 douyin-douyin1-all

# 9号：京东逐店预登录（不限时等人，登录态落盘）+ 自动补齐京东 + 金山同步
#      关键价值：采集流程内置人工等待只有 15 分钟，人不在就整店失败；本脚本把登录与采集解耦
cd "9.客服数据自动更新" && node scripts/preloginJdThenCollect.js
#      调试用：JD_PRELOGIN_ONLY=1 只预登录；JD_PRELOGIN_LIMIT_MS 改每店等待上限

# 5号：非交互下载并分析（等价 TUI「7 下载」）
cd "5.电话漏接分析/电话漏接分析后台" && python scripts/auto_download_and_analyze.py
```

## 五、待用户拍板（用户 2026-09-14 明确“先不管”，已挂起；等用户主动提出再动）

> 用户原话（10:15）：“P3 排队阶段没有呼损就先不管。其他你说的几个问题先不管。”

1. **【P4 已解决】金山在线「数据明细」**：现在在线=本地=**90 行（9 家店）**、日期 **09-01~09-12**、透视筛选 **2026-09-12**。若想把 8/25 前的历史也留在在线表，需先把历史补回本地表（本地表目前只有当期）。
2. **P3 呼损**：已按用户口径收窄 —— **排队阶段无呼损 → 不管**（昨天 09-13 的 7 条全是 IVR 阶段）。
3. **P2 发票**：天猫 7 单状态=「待处理（未安排）」，未做任何平台操作（用户未要求推进）。如需：在 TUI 里逐单按 `a` 标记「已安排客服跟进」；3 单「待同意」可在天猫后台点同意；正式回传会真实提交，默认不做。
4. **P6 企微反馈**：报告已出，**未发送**（需用户确认文案 + 口令 `161323`）。
5. **两处登录**：天猫店铺8（需人工验证）——属 2.发票自动化 项目。**【09-14 11:30 更新】抖音已解决**：抖音是「**一个登录账号管多店**」（两店手机号都是 19925378376 → 同一资料目录 `account-270b8179d0d7`），换店靠切店能力，**不存在“店铺5 单独登录失效”**；今天 11:30 短信验证码重新登录成功（见「五之二」）。
6. **【09-14 11:35 新问】抖音发票「识别日期」不是今天**：已查明并处理（见下方「五之二」）；**A（显示口径）已于 12:0x 改完并推送（`5187e62`）；B（空读取保护）待用户拍板**。

## 五之二、抖音发票「识别日期」为什么不是今天（2026-09-14 查明）

- **列来源**：`2.发票自动化/共享CLI/tui/镜像表格.js:266`（表头）/ `:210` 取值 = `订单?.addedAt || 订单?.createdAt || 订单?.updatedAt`（转本地时区日期）。
- **抖音记录里没有 `addedAt`**：只有京东自己的库写 `addedAt`（`2.京东发票回传/src/order/jdOrderRecordStore.js`）；抖音/天猫走共享 `共享订单状态/orderRecordStore.js`，只写 `firstSeenAt`/`lastSeenAt`/`updatedAt` → **抖音这一列显示的其实是「最后更新时间」**，不是「识别日期」。
- **抖音本地只有 2 单**（都是店铺5 `douyin-store-2`，工作流=processing/已安排客服）：
  - `6929141403556740725`｜平台申请 2026-09-09｜¥1064.99｜回传尝试 error：`下载失败：page.evaluate: TypeError: Failed to fetch`
  - `6955623700623463456`｜平台申请 2026-09-08｜¥2397.04｜回传尝试 skipped：诺诺发票系统无发票可下载（缺 1 张）
  - 两单 `firstSeenAt`=2026-09-09 09:19:29（本地）、`updatedAt`=2026-09-09 09:22 → **显示 09-09**
  - 现场截图：`6.抖音发票回传/runtime/screenshots/douyin-invoice-return-douyin-store-2-*-20260909-*.png`（页面显示「待开票 2」）
- **【09-14 11:30 实测结论（覆盖上面“卡在登录”的初判）】**：
  - **登录态确实失效过**：跑只读同步时落在登录页，程序自动填入手机号并发出短信验证码，日志 `等待人工完成验证码或扫码`，等待上限 **15 分钟**（`src/invoiceReturn/douyinInvoicePage.js:168`）→ 所以 09-09 之后每次跑都卡在这里、没有任何产出。今天 11:30 一次人工短信验证后恢复。
  - **切店能力实测可用**（用户说对了）：`德迩杰官方旗舰店(51163399)` → `德达医疗康养器械旗舰店(162329841)` → `DEDAKJ医疗器械旗舰店(29502951)`，全自动切店成功，代码在 `src/browser/douyinStoreIdentity.js`（注释：从项目 12 照抄的成熟方案）。
  - **只读同步今天跑通**：`node .codex-temporary/readonly-sync.js douyin` → **两店都成功、都是 0 单**（新增 0/更新 0）。
  - **平台事实（截图 + 「全部」列表存证）**：`runtime/screenshots/today-check-douyin-store-2-*.png`；店铺5 「待开票 0」；那两单**早在 09-12 就已开票**：
    - `6929141403556740725`（¥1064.99）→ 已开票，开票完成 **2026/09/12 15:24:51**
    - `6955623700623463456`（¥2397.04）→ 已开票，开票完成 **2026/09/12 15:25:40**
  - **本地队列已按设计清空**：抖音按「平台当前快照」处理（`douyinOrderRecordStore.js` 注释：旧订单不应继续进入回传队列），所以 `data/invoice-order-records.json` 现在 **0 条**（11:30:34 写入）。
- **完整因果**：09-09 抓到 2 单待开票 → 回传失败（下载中心/诺诺取不到票）→ 09-09 之后登录态失效，同步一直卡在登录页 → 本地记录停在 09-09（且该列对抖音取的是「最后更新时间」而非 `firstSeenAt`）→ 09-12 财务人工开票 → 今天同步跑通，平台已无待开票，队列清空。
- **待用户拍板**：
  - **✅ (A) 显示口径【已完成 2026-09-14，commit `5187e62`】**：`共享CLI/tui/镜像表格.js:210` 取值改为 `addedAt || firstSeenAt || createdAt || updatedAt`（抖音/天猫从「最后更新时间」改为「首次识别时间」；京东自带 `addedAt` 行为不变）。测试全绿：镜像表格 9/9、回传平台TUI 13/13、京东开票巡检 tuiPages 7/7。
    - 遗留口径（未改，未拍板）：京东的 `addedAt` 是「最后一次被重新加入队列的时间」（`jdOrderRecordStore.js:196` 用 `record.addedAt || now` 保留，但订单离开队列后重进会刷新，实测老单 addedAt=09-14 而 firstSeenAt=08-21）。若想让京东也显示真正首次识别，把 `firstSeenAt` 提到 `addedAt` 前面即可（会改动京东现有显示口径与测试）。
  - **✅ (B) 队列清理口径【用户 2026-09-14 拍板：维持现状】**：用户定为「TUI 完全镜像浏览器（平台）里的内容」——平台是空就清空本地队列，不加防误删保护；用户用“浏览器窗口留着自己看一眼”来把关。实测四平台**跑完都不会自动关浏览器**（无需改代码）：
    - 抖音 `src/app/syncPendingOrders.js` finally：注释「浏览器保持打开，供人工核实……不在此自动关闭」
    - 天猫 `src/app/syncPendingOrders.js` finally：注释「同步与正式回传复用同一个浏览器上下文……继续保持打开供人工核实」
    - 拼多多：只读同步阶段会关自己的上下文（注释：让紧随的正式回传接管同一资料目录），但任务结尾的回传会留窗（`returnInvoiceToPdd.js` / `loginStores.js` / `collectPddElements.js` 均注明「浏览器保持打开，供人工核实」）
    - 京东：任务服务传 `批量店铺页面保留模式/批量回传页面保留模式 = 'keep'`（`taskConstants.js`）；1.京东开票巡检 TUI/CLI 传 `巡检后保持页面打开: true` → 'wait' 模式＝页面保持打开、等你手动关窗
    - ⚠仅有的会关场景：用一次性脚本（如 `.codex-temporary/readonly-sync.js`）跑——进程退出浏览器随之关闭。要看现场请用 TUI 跑。
  - **✅ (C) TUI 顶部显示上次同步【已完成 2026-09-14，commit `35d612f`】**：新增 `共享CLI/上次同步记录.js`（写/读/拼文案，读取单数从任务消息里累加），任务结束自动写 `<项目>/runtime/state/last-sync.json`，共享 TUI 总览页多一行「上次同步：YYYY-MM-DD HH:mm（读取 N 单）」（无记录时不显示）。已接入四平台（抖音/天猫/拼多多/京东发票回传）。测试：上次同步记录 5/5、回传平台TUI 15/15、抖音全套 101/101。
  - **✅ (C-2) 京东开票巡检（1号）也接上了【2026-09-14】**：它不走共享模板，已在自己的总览页加同款一行「上次巡检：YYYY-MM-DD HH:mm（识别 N 条｜新增 N 条｜告警 N 条）」，单店/批量巡检结束时写入 `1.京东开票巡检/runtime/state/last-sync.json`。测试 158/158。已把 09-12 那次真实结果回填，重开 TUI 就能看。
  - **📌 巡检执行情况（用户 2026-09-14 问"今天到底跑了没"）**：**1号京东开票巡检今天没跑**——最后一次是 **2026-09-12 08:48**（5/5 店成功，识别 0 条、新增 0、告警 0）；数据文件（`store-results.json`/`invoice-order-state.json`/`latest-invoices.json`）都停在 09-12。**2号京东发票回传的催票识别今天 09:00:17 跑过**（京东5S店，后台票据 44 单、催票 1 单，日志 `2.京东发票回传/runtime/latest-run.log`）。要刷新 1号巡检数据需在它 TUI 总览页回车「批量巡检」（浏览器会弹，登录态失效就停在登录页等人）。

## 六、常用命令

```bash
# P1 新鲜度自检（客户页同一算法）
cd "1.客服超时督办" && node -e "const{readMissedReplyMonitorState}=require('./src/features/missedReplyMonitor/missedReplyStateStore');const{buildCustomerMirrorItems}=require('./src/controlCenter/customerMirrorListBuilder');const{buildDataFreshness}=require('./src/controlCenter/controlCenterDashboardService');const n=Date.now();const st=readMissedReplyMonitorState();const it=buildCustomerMirrorItems(st,n);const f=buildDataFreshness(it,n);console.log('新鲜度='+Math.round(Math.max(0,Math.min(1,1-f.ageSeconds/120))*100)+'% 年龄='+f.ageSeconds+'s 客户='+it.length)"

# P2 发票只读同步（不提交）
cd "2.发票自动化" && node .codex-temporary/readonly-sync.js all

# 抖音发票记录时间字段速查（首次识别 vs 最后更新）
cd "2.发票自动化/6.抖音发票回传" && node -e "for(const o of Object.values(require('./data/invoice-order-records.json').orders||{}))console.log(o.orderNumber,'首次识别',new Date(o.firstSeenAt).toLocaleString('zh-CN'),'最后更新',new Date(o.updatedAt).toLocaleString('zh-CN'))"

# 抖音发票页只读核查（数订单 + 点标签 + 截图存证，不导出不回传）
cd "2.发票自动化" && node .codex-temporary/douyin-page-check.js douyin-store-2 全部
# 若跑批卡在登录：看 6.抖音发票回传/runtime/latest-run.log 是否在打印「等待人工完成验证码或扫码」，去可见窗口输短信码即可（15 分钟窗口）
```

## 七、注意事项（踩坑）

- **端口冲突**：9号项目与19号项目都用 **9333**；12号项目用 9334。跑 9号项目采集前必须先关掉 19号项目的 Edge，否则 9号会报「调试端口 9333 不属于本项目浏览器」并让整店失败。
- 关掉 19号项目的 Edge 不影响其登录态（存在 `runtime/browser-profile`）。
- P1 的督办进程与 9/12 号项目的采集可共存（P1 用自己的 Edge 资料目录）。
- 京东 3 家店在 9号项目里各自独立资料目录；登录态会落盘，但**京东会不定期要求重新安全验证**，届时用 `preloginJdThenCollect.js` 最省事。
- `.gitignore` 只收源码：`runtime/`、`*.xlsx`、`*.png`、密钥配置都不入库，所以本次直调脚本已从 `runtime/` 挪到 `scripts/` 才提交。
