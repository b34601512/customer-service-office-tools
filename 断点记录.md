# 断点记录（多项目自动化执行任务）

> 覆盖更新，只保留最新一份。位置：`D:\桌面\办公软件\断点记录.md`
> 最近更新：2026-09-14 10:50 ｜ 状态：**P1~P6 全部完成；无未完成的待办**（下方“待用户拍板”项已由用户明确“先不管”，不算未完成）

## 一、任务来源

用户要求按顺序自动化执行 6 个项目（P1~P6），逐项验收并汇报；不要用后台监视隐藏执行；
遇到必须人工的登录/滑块要停下来叫人；业务口径不猜。

## 二、各阶段状态（全部已完成）

| 阶段 | 状态 | 关键事实 / 产物 |
|---|---|---|
| P1 1.客服超时督办 | ✅ 通过 | `node src/main.js run`（= TUI「后台启动」，真源 `src/controlCenter/taskService/taskConfig.js:18`），PID **8588**（08:37 启动）。终检新鲜度 **98%**（验收线 90%，阈值 120s）；客户判定 94；待回复候选 2 条（都是 10–14 天前已提醒过的历史遗留，已提醒不重复）。用户确认不改 TUI。 |
| P2 2.发票自动化 | ✅ 已汇报（待用户定是否推进） | 只读巡检。**关键结论：天猫 7 单全部是「待处理（未安排）」**（工具里的客服跟进状态字段：pending=待处理/processing=已安排客服跟进/发票已登记/已处理）；其他平台已安排：京东开票巡检 4 单=已安排、京东催票 4 单=已安排+7 单已处理、抖音 2 单=已安排、拼多多 0 单。 |
| P3 5.电话漏接分析 | ✅ 已完成（口径已按用户要求收窄） | 下载 2026-06-17~09-14；**用户要求只看昨天（09-13）+ 排队阶段** → 实测昨天 7 条呼损**全部是 IVR 阶段**（排队停留 0 秒），**排队阶段 0 条**（最近一次排队阶段呼损是 09-08）。7 个号码已列出，等用户确认口径（严格排队阶段 vs 含 IVR）。 |
| P4 9.客服数据自动更新 | ✅ 完成（已修正） | 首轮 6 成功/3 失败（京东店登录问题），修 2 个京东缺陷后补齐。**用户随后指出“金山没有最新数据”**：根因是**我直调汇总函数时跳过了 TUI/CLI 启动才做的日期重算** → 沿用了配置里的旧范围 2026-09-01~09-10，而智能模式应为「本月1号~今天-2天」= **09-01~09-12**。已在直调脚本补上 `initializeProjectConfigForStartup()`，重跑后：本地 `数据明细` **90 行 = 9 家店齐**（天猫2+京东3+拼多多2+抖音2），全部 **2026-09-01~09-12**；金山 **在线 90 = 本地 90**；透视筛选日期 **2026-09-12**，**22/22 成功**。另外修了抖音广告弹窗拦截切店点击的缺陷（见下）。 |
| P5 12.店铺指标数据自动更新 | ✅ 完成 | 汇总 **9/9 家成功**，金山一键同步数据源 **在线 232 行 = 本地 232 行逐格一致**，耗时 221s。数据源：`outputs/019fbb96-.../店铺指标数据源.xlsx`。 |
| P6 19.聊天质检 | ✅ 报告已出 | 取 2026-09-14 售前会话 12 条（京东客服管家，独立 Edge 端口 9333），按 sid 分段落盘 `runtime/chat/2026-09-14_*__<sid前8位>.chat.json`；AI 已逐条通读并出报告。**企微未发送**（需用户确认文案 + 口令 `161323`）。 |

## 三、本次修过的代码（已提交并推送）

| 提交 | 内容 |
|---|---|
| `4be57bf` | 京东登录页/安全验证不再被误判为遮挡弹窗（`hasJdLoginOrVerificationText`；`stabilizeJdBrowser` 跳过登录/验证面；人工验证正则补「拖动…拼图/箭头」） |
| `32544d8` | 京东登录填表竞态不再让整店失败（fill 限时 5s；「页面已跳走/元素脱离/上下文销毁」识别为竞态收敛返回 false，不吞真实错误） |
| `ea77ca8` | 新增 3 个非交互直调脚本（见下） |
| `90e1e0c` | **直调脚本补上启动日期初始化**（修「数据不是最新」根因）；**抖音广告弹窗拦截切店点击**：新增 `douyinAdPopupDismiss.js`，只在 clicks 报「intercepts pointer events」时定向关广告弹层（唯一明确关闭入口，否则 ESC 收起，绝不点促销 CTA），关不掉就报错停下 |
| `bbe2340` | 断点记录 + 发票只读同步/巡检脚本 |

## 四、新增可复用脚本

```bash
# 9号：全店汇总 + 金山同步 + 透视筛选日期（含启动日期重算——缺了它数据就不是最新）
cd "9.客服数据自动更新" && node scripts/runSummaryAndKdocsSync.js

# 9号：只补指定店铺（缺省=京东6店/拼多多03/德达抖音），同样含启动日期重算+金山同步
cd "9.客服数据自动更新" && node scripts/retryFailedStores.js [任务ID ...]
#      任务ID 见 scripts/runSummaryAndKdocsSync.js 输出或 TUI 店铺页；如 douyin-douyin1-all

# 9号：京东逐店预登录（不限时等人，登录态落盘）+ 自动补齐京东 + 金山同步
#      关键价值：采集流程内置人工等待只有 15 分钟，人不在就整店失败；本脚本把登录与采集解耦
cd "9.客服数据自动更新" && node scripts/preloginJdThenCollect.js
#      调试用：JD_PRELOGIN_ONLY=1 只预登录；JD_PRELOGIN_LIMIT_MS 改每店等待上限

# 5号：非交互下载并分析（等价 TUI「7 下载」）
cd "5.电话漏接分析/电话漏接分析后台" && python scripts/auto_download_and_analyze.py
```

## 五、待用户拍板（用户 2026-09-14 明确“先不管”，已挂起；等用户主动提出再动）

> 用户原话（10:15）：“P3 排队阶段没有呼损就先不管。其他你说的几个问题先不管。”

1. **【P4 已解决】金山在线「数据明细」**：现在在线=本地=**90 行（9 家店）**、日期 **09-01~09-12**、透视筛选 **2026-09-12**。若想把 8/25 前的历史也留在在线表，需先把历史补回本地表（本地表目前只有当期）。
2. **P3 呼损**：已按用户口径收窄 —— **排队阶段无呼损 → 不管**（昨天 09-13 的 7 条全是 IVR 阶段）。
3. **P2 发票**：天猫 7 单状态=「待处理（未安排）」，未做任何平台操作（用户未要求推进）。如需：在 TUI 里逐单按 `a` 标记「已安排客服跟进」；3 单「待同意」可在天猫后台点同意；正式回传会真实提交，默认不做。
4. **P6 企微反馈**：报告已出，**未发送**（需用户确认文案 + 口令 `161323`）。
5. **两处登录**：天猫店铺8（需人工验证）、抖音店铺5（登录失效）—— 属 2.发票自动化 项目，与 9 号项目的抖音采集无关。

## 六、常用命令

```bash
# P1 新鲜度自检（客户页同一算法）
cd "1.客服超时督办" && node -e "const{readMissedReplyMonitorState}=require('./src/features/missedReplyMonitor/missedReplyStateStore');const{buildCustomerMirrorItems}=require('./src/controlCenter/customerMirrorListBuilder');const{buildDataFreshness}=require('./src/controlCenter/controlCenterDashboardService');const n=Date.now();const st=readMissedReplyMonitorState();const it=buildCustomerMirrorItems(st,n);const f=buildDataFreshness(it,n);console.log('新鲜度='+Math.round(Math.max(0,Math.min(1,1-f.ageSeconds/120))*100)+'% 年龄='+f.ageSeconds+'s 客户='+it.length)"

# P2 发票只读同步（不提交）
cd "2.发票自动化" && node .codex-temporary/readonly-sync.js all
```

## 七、注意事项（踩坑）

- **端口冲突**：9号项目与19号项目都用 **9333**；12号项目用 9334。跑 9号项目采集前必须先关掉 19号项目的 Edge，否则 9号会报「调试端口 9333 不属于本项目浏览器」并让整店失败。
- 关掉 19号项目的 Edge 不影响其登录态（存在 `runtime/browser-profile`）。
- P1 的督办进程与 9/12 号项目的采集可共存（P1 用自己的 Edge 资料目录）。
- 京东 3 家店在 9号项目里各自独立资料目录；登录态会落盘，但**京东会不定期要求重新安全验证**，届时用 `preloginJdThenCollect.js` 最省事。
- `.gitignore` 只收源码：`runtime/`、`*.xlsx`、`*.png`、密钥配置都不入库，所以本次直调脚本已从 `runtime/` 挪到 `scripts/` 才提交。
