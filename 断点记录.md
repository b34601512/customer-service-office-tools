# 断点记录（多项目自动化执行任务）

> 覆盖更新，只保留最新一份。位置：`D:\桌面\办公软件\断点记录.md`
> 最近更新：**2026-09-16 22:05** ｜ 状态：**1号「超时自动转接」后台运行中；全工作区截图/壳/分发包已清理完毕（分发包 2.1GB 已搬 D:备份文件夹办公软件产物备份_20260916）；待拍板：P2 发票12单、P3 IVR12条、打包脚本是否删**

## 〇、下次开工第一步（照做即可）

1. 读本文件（就是这份）。
2. 检查 1号后台是否在跑：
   ```powershell
   Get-CimInstance Win32_Process -Filter "Name='node.exe'" | Where-Object { $_.CommandLine -like '*src/main.js run*' } | Select-Object ProcessId,CreationDate
   ```
   不在就跑（见第五节命令）。
3. 看 1号 转接健康度：
   ```bash
   cd "D:/桌面/办公软件/1.客服超时督办"
   grep -nE "已发送转接指令|转接已核验|转接未生效|发送转接失败通知|交班补判|主线:失败" runtime/current-run.log | tail -40
   ```
   - 「转接已核验」正常；「转接未生效」/「发送转接失败通知」= 群里已@主管 → 查根因。
   - 夜里 22:30（售后）/23:45（售前）之后应**完全静默**，不该有转接或告警。
4. 自检：`cd 1.客服超时督办 && npm test`（**基线 445**）必须全绿。

## 一、1号「超时自动转接」当前状态（2026-09-16 收工）

**后台进程**：PID **38804**（17:00:09 启动），失败日志 **0** 条；登录态正常，上班监控在线快照正常（收工时：叶炳辉/缪婷婷/柯紫婷在线，韩欢欢未上线）。

**口径（用户 2026-09-16 定稿，唯一核心：客户消息必须有人回）**：

- **触发转接**：原接待已过自己班次时间窗（早班 08:00~16:30；售前晚班 15:45~23:45；售后晚班 14:00~22:30）**或**在班但没上线（接单开关关着）；运营账号一律按没人管。
- **目标**：**同组**（售前→售前、售后→售后、运营→售前）+ **当天当班** + **已上线**（售前看「可被转接」、售后看「自动分配」），按排班表从上到下取第一个；**不看值班背景色/组长/值班标记**（颜色只剩 `排班颜色清单` 诊断日志）。
- **原接待在线状态查不到**（快照过期/缺人）→ 维持现状不转，避免误伤正在忙的人。
- **当班的人都不可接** → 不转 + **@主管**；非工作时段不转不发群。
- **交班补判**：主循环每 5 分钟对“真的分配到人 + 客户还在等回复”的会话补判一次（兜住“提醒发过之后原接待才下班、不会再有新提醒”的客户）；同一客户 30 分钟冷却、原接待换人立刻重判；只在真正动手（发出转接/触发@主管）时记账。

**验收记录**：

| 项 | 应有 | 实有 |
|---|---|---|
| 自动化测试 | 全绿 | **445/445** |
| 首例真实全链路转接 | 提醒→判定→转接→核验→通知 | **2026-09-16 12:00:42** 客户黎路遥：柯紫婷（售后晚班，已过班）→ 李守耀，2 秒核验通过，企微群已通知 |
| 在班不误转 | 在班且在线的原接待不转 | 11:15「一路有你」、13:18 李守耀、15:00 重叠期——均正确跳过 |
| 在班没上线也转 | 应转给当班在线的人 | 已实现并有测试（源在班但开关关着→转；状态查不到→不转） |
| 交班补判 | 提醒发过、原接待才下班的客户要补判 | 已实现并在跑（收工时候选=0，没有等回复的客户） |

## 二、下次开工可继续做的（非必须）

- 继续只读盯 1号：重点看**早班 8:00 接班**与**16:30 早班下班**两个时点的转接表现；出现「转接未生效/失败通知」先查根因。
- 可选清理：临时脚本 `C:\Users\b3460\AppData\Local\Temp\auto-transfer-live-test.js`、`probe-attach-readonly.js`（本次联调用，已不再需要）。

## 二·补、截图凭证已全工作区删除（2026-09-16 全天，用户决定：先「统一砍掉」、再补一句「砍」）

**用户口径**：截图全部砍掉。看不了后台就不看，改为**看最终统计数据**（数据大差不差即可）。历史截图**全部移到备份文件夹**，用户有空手动删。

**为什么要砍**（实测根因，不是为了省磁盘）：
- `page.screenshot({ fullPage: true })` 会先等页面字体加载完；字体请求挂起时死等到 30 秒超时抛错，**把已经成功的店铺判失败**。
- 历史 33 天采集日志里 **5 天**出现该致命失败（08-21、08-24、08-26、09-08、09-11）+ 09-16；另有 3 个批次卡在「**下载后**」——那时文件已拷到正式目录，**等于白丢一份已拿到的数据**。
- 9号 是**无头采集 + 每家店跑完必关浏览器**（9 家店共用 9333 端口，端口不释放会直接抛「不能继续拉起新店浏览器」），所以**没有「留页面给人工看」这条路**；砍完就是真的不看图了。

**代码改动（已提交推送 `fc4448a`）：不再用开关，截图采集链路整条删除**

> 用户看过第一版实现后指出：加「总开关」是半刀，项目依旧不够简洁。核实成立——开关只买到可逆性，却留下 267 行截图代码 + 10 个「进去就 return」的调用点 + 一份为开关自造的测试。既然截图永远不要了，就删能力本身。**净删 614 行 / 加 42 行（26 个文件）**。

| 位置 | 改动 |
|---|---|
| 9号 采集链路 | **整删 4 个文件**：`shared/evidenceSettings.js`、`shared/downloadEvidence.js`、`platforms/pdd/pddEvidenceCapture.js`、`tests/evidenceScreenshotsDisabled.test.js` |
| 9号 调用点 | 清掉 10 处（抖音下载前/后、京东下载前/后、拼多多下载前/后、天猫目标页/日期已确认/下载前/下载后）；拼多多那两处原先还会**白关弹窗 + 白滚动页面** |
| 9号 中间层 | 删掉因此无消费者的 `buildTmallDownloadEvidenceLabel`、`evidenceOptions`、以及 `evidenceDir / evidenceFileNamePrefix` **整条管道**（runner → downloader → 平台下载器） |
| 9号 天猫核对点 | 只写页面地址 json（新日志节点「天猫核对点/页面地址」），png 分支消失 |
| 9号 失败现场 | 只写「失败原因.txt」，不再连浏览器；`pickSummaryEvidencePage` 一并删除 |
| 2号 催票巡检 | 删 `common/screenshotEvidenceSettings.js` 与 `保存轻量截图`；巡检不再收 截图文件名/截图路径/失败截图路径；失败现场只留 标题/地址/正文预览 文本 |
| 2号 任务层 | `taskServiceCore` 清掉巡检的凭证管道（单店识别/批量识别/登录恢复 三处的 凭证批次目录 + 截图场景 + 构建店铺凭证路径 注入），并删除已无调用者的 `构建店铺凭证路径` |

**刻意保留（别当疏漏）**：
- 失败原因.txt、天猫 checkpoints 的页面地址 json、各大平台采集的页面文本凭证（URL/TITLE/正文）、`evidenceNaming.js`（txt 命名仍用）。
- 下面这些是「只为显示截图而存在的壳」，**已列入待用户拍板**（还没动）：各项目 `screenshotPath` 结果字段、`截图目录 paths.js / evidenceService / evidenceViewer`、2号 TUI「打开凭证文件夹」菜单与 controlCenter 取图端点。

## 二·补·2、第二批：发票回传 + 12号（2026-09-16 傍晚，用户回「砍」）

**提交**：`adc652a`（发票回传 5 处）、`0731cda`（12号）

第一批砍完后，用户对「4.天猫/5.拼多多/6.抖音/3号诺诺登录」回「砍」。执行时又扫出漏网的 **12号**（跟 9号 是同一份截图代码的孪生，都有 `shared/downloadEvidence.js` + `fullPage: true`），一并清掉。

| 项目 | 改动 |
|---|---|
| 2号 `jdInvoiceUploader` | 删 `构建回传截图路径` + `保存回传截图`（含 成功/已开票/失败 三处调用） |
| 4号 天猫回传 | 删 `格式化截图时间`/`构建天猫回传截图路径`/`保存天猫回传截图`/`保存下载阶段凭证截图`；清掉无消费者的 `screenshotPath` 字段与死参数 `page` |
| 5号 拼多多回传、6号 抖音回传 | 同上；抖音那版还带「重试 3 次后才抛 保存抖音回传截图失败」的逻辑，一并删 |
| 3号 通用下载中心 | 诺诺登录验证不再截图，CLI 不再打印「[证据] 登录截图：…」（`loginVerifier.js` + `commandLineOperations.js`） |
| **12号** | 删 `src/shared/downloadEvidence.js`（本就无调用者的死代码）；jd/tmall/pdd/douyin 四个 collector + `storeMetricsShared` 的 png 全删，**文本凭证全部保留**（天猫原来只有 png，现改为落「真实体验分-读取成功.txt」，与其他平台口径一致） |

**顺带清掉的死东西**：两处只为截图而存在的 `try/catch（catch 里只 throw）`、`evidenceName` 参数、`error.evidencePath` 赋值、已无人用的 `fs`/`buildEvidenceFilePath`/`验证凭证文件`/`规范化凭证名称` 引用。

**测试改动原则**：不把断言删弱——把假 page 的 `screenshot` 改成「一被调用就抛错」，再断言流程照常走完且 `item.screenshotPath === undefined`。

**验收记录（应有 vs 实有）**：

| 项 | 应有 | 实有 |
|---|---|---|
| 五项目单测 | 全绿 | 2号 **130/130**、3号 **61/61**、4号 **100/100**、5号 **76/76**、6号 **101/101**、12号 **151/151** |
| 12号 真跑（TUI 同源的「强制采集本店」，天猫1店） | 数据落盘 + 零截图 | ✅ `VERIFY12=[{平台:tmall,店铺:天猫1店,状态:success,指标数:24}]`；新增截图 **0**；文本凭证落盘 `runtime/evidence/store-metrics/2026-09-16-19-33-28-800-天猫-天猫1店-tmall1/真实体验分-读取成功.txt` |
| 全工作区扫描 | 源码无截图调用 | ✅ `grep -rn "page.screenshot" --include=*.js`（排除 dist/venv/备份）= **0** |

> 副作用（正向）：那个「截图卡死弄死店铺」的缺陷（约占 18% 的日子）**随本次改动直接消失**，不再需要单独修。

**历史凭证搬迁**（已执行）：
- 3263 个文件 / **924MB** → `D:\桌面\办公软件\历史凭证备份\截图凭证_20260916\<项目>\...`（保持原目录结构）
- 搬迁脚本：`_tools/搬迁历史截图.sh`（可重跑）；`.gitignore` 已加 `历史凭证备份/`
- **刻意没动**：浏览器资料自带的图标资源（9号 1729 张）、3号 `python_env` 资源、**19号 `runtime/review/media-*`（客户在聊天里发的图片，是业务数据）**、各项目 evidence 目录里的 .txt/.json 证据
- 用户手动删除时机：他自己有空再删 `历史凭证备份/` 整个目录即可

**验收记录（真实产物）**：

| 项 | 应有 | 实有 |
|---|---|---|
| 9号 强制重下真跑（京东3店） | 数据落盘、无截图 | ✅「本次汇总全部完成：共 1 家」；新文件 `20260916-174457-京麦接待数据...xlsx`；新增截图 **0**；截图字样 **0** |
| 9号 强制重下真跑（天猫1店，含核对点路径） | 同上 | ✅「全部完成：共 1 家」；新下载 3 个 .xlsx；新增截图 **0**；checkpoints 目录 png **0**、json 正常；新日志「天猫核对点/页面地址」正常输出 |
| 9号 单测 | 全绿 | **25/25**（fail 0） |
| 2号 单测 | 全绿 | **130/130**（fail 0） |
| 2号 巡检失败诊断（真浏览器） | 巡检失败时不再截图、只留文本、不崩 | ✅ 用本地假网页（`http://127.0.0.1:8123/`）+ 假店铺 id 跑 `执行巡检`：在 try 内部失败并走到 catch；日志 `[失败诊断] 标题=验证假页面；URL=…；预览=…`（**已无「截图=」字段**）；`pageDiagnostic` 无 screenshotPath；新增截图 **0**；零残留 |
| 2号 完整只读巡检 | — | ❎ 作废：卡在京东3店登录要滑块，改用上面那条假页面验证（同一条失败路径且不需登录），**因此不需要用户滑滑块**；卡住的跑批已停，避免它到点写一条误导性的“失败”记录 |
| 源目录残留 | 0 张图 | evidence / checkpoints / screenshots 剩余图片均为 **0**，文本 json 保留 |

> 副作用（正向）：那个「截图卡死弄死店铺」的缺陷（约占 18% 的日子）**随本次改动直接消失**，不再需要单独修。

## 二·补·3、第三批：截图「壳」也拆了（2026-09-16 晚，用户回「这个也不要截图了，壳拆」）

**提交**：`2bf46ec`（59 个文件）

截图本身删完后，还留着一圈“只为截图服务”的空壳（会打开空文件夹的菜单、永远为空的字段）。用户拍板：拆。

| 项目 | 拆掉的东西 |
|---|---|
| **2号** | 整删 `common/evidenceService.js`（凭证批次目录 创建/查找/校验/统计 一整套）与 `cli/evidenceViewer.js`；菜单去掉「凭证查看/打开凭证文件夹」；`startCli` 不再拼「凭证可用/缺失」；**`凭证批次目录` 整条管道**（taskServiceCore→returnInvoiceToJd→jdInvoiceUploader，已是无写入者的空转）；结果/订单记录去掉 `screenshotPath`、`invoiceReturnScreenshotPath`；**打包脚本不再创建空 `runtime/screenshots`** |
| **3号** | paths/fs 去掉 截图目录 |
| **4/5/6号** | paths/fs/启动清理 去掉 截图目录；删 `cli/evidenceViewer.js` 与菜单「凭证查看」空组、打开凭证文件夹入口 |
| **共享层** | `共享订单状态`/`共享发票回传`/`共享CLI` 去掉 screenshotPath 字段与「截图：」显示行 |
| **12号** | 仅改 README（说明凭证=文字）；**TUI「打开凭证文件夹」保留** —— 那目录里是真实 `失败原因.txt`，不是空壳 |
| **9号** | 不动：凭证菜单里是 58 份 `失败原因.txt`，仍是活的功能 |
| **10号自动报量** | **不动**：它的“截图”是报量结果图，属于对外产物，不是采集凭证 |

**测试同步改成反向断言**（不删弱）：4/5/6号 的架构边界测试由「截图路径定义保留」改为「已彻底移除」，启动清理测试改用真实清理项（导出目录）验证。

**验收（应有 vs 实有）**：

| 项 | 应有 | 实有 |
|---|---|---|
| 六项目单测 | 全绿 | 2号 **125/125**、3号 **61/61**、4号 **100/100**、5号 **76/76**、6号 **101/101**、共享模块 **52/52** |
| 2号 真跑（假网页+假店铺，真浏览器失败路径） | 失败可诊断、零截图 | ✅ `[失败诊断] 标题=验证假页面；URL=…；预览=不是京东后台`；结果对象**无截图字段**；新增截图 **0**；新增凭证目录 **0** |
| CLI 真界面渲染 | 菜单无凭证入口、能正常退出 | ✅ 2/4/5/6号 菜单实测：无「凭证查看」组；3号 7 个菜单项正常 |
| 磁盘残留 | 无空 screenshot 目录 | ✅ 6 个 `runtime/screenshots` 空目录已删（内容早已搬去备份） |

## 二·补·4、第四批：分发包全搬走 + 两处凭证/结果图也搬走（2026-09-16 晚，用户：「不要分发包了，以后直接源代码发给他」）

**搬运结果：6701 个文件 / 2127MB → `D:\备份文件夹\办公软件产物备份_20260916\`**（保持原目录结构；源目录已清）

| 搬走的 | 大小 | 以后怎么用 |
|---|---|---|
| `2.发票自动化/dist`（客服版 v0.01） | 917MB | 源码就在仓库里，直接给源码 |
| `2.发票自动化/2.京东发票回传/dist`（客服版 v0.02） | 136MB | 同上 |
| `3.响应时间/dist` + `发布包` | 280MB | 双击 `一键启动.bat`（走 `launcher.ps1`→源码） |
| `8.抖音直播评论家/dist` + `release_package` | 305MB | 双击 `一键启动.bat`（走 `app_entry.py`） |
| `11.视频压缩工具/dist` + `build` + `release` | 133MB | 双击 `启动程序.bat`（走 `.venv` 里的 python + `main.py`） |
| `9.客服数据自动更新/发布包` | 39MB | 直接用仓库源码（平时就是这么跑的） |
| `16.BOSS直聘获取公司名称/发布`（4 个目录 + 5 个 zip） | 289MB | 双击 `启动BOSS采集.bat` |
| `10.自动报量/html导入工具/自动报量-v0.5.zip`、`v0.6.zip` | 3MB | 代码不引用；真要旧版自己去备份拿 |
| **9号 `runtime/evidence`**（58 份失败原因.txt 等） | 0.2MB | 凭证文件夹菜单保留，下轮失败会重新生成 |
| **12号 `runtime/evidence`**（各店页面文字凭证） | 1.4MB | 同上 |
| **10号 `自动报量输出/截图`**（7 张报量结果图） | 22MB | 下一轮报量会重新生成（那是发给人的产物） |

**安全核查**：搬之前确认过 4 个 Python 工具都有源码启动器（不靠 dist 里的 exe 跑）；`git status` 没出现删除 → 搬走的全是 runtime/产物，**仓库里没有任何入库文件被搬**。

**可复用工具**：`_tools/搬迁产物到备份.js`（改列表即可；已存在的备份不会覆盖，搬完逐项对账）

**待拍板**：那堆「打包用的脚本」要不要一并删？（`导出客服分发包.js`、`buildPortablePackage.js/.ps1`、`build_release.ps1`、`一键打包.bat`、`打包便携版.bat`、`packaging/`，以及相关测试）—— 它们现在只会把刚搬走的东西又生成出来。

## 三、等你拍板（遗留）

1. **P2 发票 12 单待处理**（天猫 10、拼多多 1、抖音 1），其中 3 单超 7 个工作日：
   - 天猫店铺2 `3317002428014087465` ¥1364（09-02 申请，10 个工作日）
   - 天猫店铺2 `5127724189085019444` ¥300（09-03，9 个工作日）
   - 天猫店铺1 `5127726025231004022` ¥458（09-05，8 个工作日）
   - 当时只做只读巡检，未做任何平台操作（不提交、不回传）；要不要推进？（天猫「待同意」单需在天猫后台点同意）
2. **P3 的 12 条 IVR 呼损**要不要安排回访？（按你口径「只看排队呼损」未计入）
3. **京东账号目录统一：结论是不做**。2026-09-16 核实：2号=Edge、9号=Chrome，资料目录本来就是各登各的；今天铁证——2号 京东1店失效时，9号 同一个账号好好的，**两份登录态互不拖累**。9号 京东近 3 周只要过 **2 次**人工（08-21、08-28），所以「统一」几乎省不下登录次数，代价却是两个项目都要改启动/登录逻辑 + 资料目录不能并发 + 跨浏览器 cookie 搬不了。**除非用户另有要求，否则不要再动这件事**；日常按「失效就叫用户」处理即可。
4. `自动化执行经验.md` 是否也贴一份到 `D:\SoftTalk\AGENTS.md`。
5. 1号相关：**无待拍板**（口径已定稿）；如以后想改回“必须值班人”的规则，说一声即可，代码里已无该概念，需要重新加。

## 四、1号常用命令

```bash
# 启动（隐藏窗口）
powershell -NoProfile -Command "Start-Process -FilePath 'node' -ArgumentList 'src/main.js','run' -WorkingDirectory 'D:\桌面\办公软件\1.客服超时督办' -WindowStyle Hidden"

# 停止（先取 PID 再杀）
powershell -NoProfile -Command "Get-CimInstance Win32_Process -Filter \"Name='node.exe'\" | Where-Object { \$_.CommandLine -like '*src/main.js run*' } | Select-Object -ExpandProperty ProcessId"
taskkill //PID <PID> //T //F

# 日志与健康度
cd "D:/桌面/办公软件/1.客服超时督办"
grep -nE "已发送转接指令|转接已核验|转接未生效|发送转接失败通知|交班补判|主线:失败" runtime/current-run.log | tail -40
grep "刷新在线状态" runtime/current-run.log | tail -3

# 自检
npm test          # 基线 445

# 状态文件
runtime/missed-reply-monitor/state.json      # 谁在等回复
runtime/auto-transfer-sweep/state.json       # 交班补判账本（只在真正动手时写）
```

其他项目（2号发票 / 5号电话漏接 / 9号客服数据 / 12号店铺汇总）的命令沿用本文件历史版本，最近一次记录：
```bash
# P2 2号：每日只读巡检（三步全跑 / 跳过指定步骤）
# P3 5号：菜单7 下载 + 菜单1/3 查询排队呼损（自动落盘报告）
# P4 9号：菜单2 全部汇总 + 菜单5 金山一键同步 + 透视筛选日期=最新
```
（如需完整参数，问用户或查各自项目 README。）

## 五、注意事项（踩坑，照做）

1. **9号与19号都用 9333 端口，必须串行**；12号 9334；1号 9371 是交行 U 盾驱动占用，**不是浏览器调试端口**，无法 CDP 附加。
2. **1号要动浏览器做联调，必须先停后台**（同一登录态/用户目录会被占用；停机约 2 分钟，状态会持久化，重启后提醒照常触发）。
3. **测试必须用 `npm test` 跑**；不要用 `node -e "require('./tests/...')"` 直接加载测试文件——那会把测试数据写进真实 `runtime/` 状态（本次误写一条交班补判账本记录，已清理）。
4. **Windows 上进程用 `process.execPath` + 不开 shell**；`node --test` 连续跑会在生产目录产生环境差异（`groupId` 会解析成真实配置），不要在生产目录直接跑。
5. **每个会等人的步骤都要有“无输出上限”**：单店/单步骤超过 ~5 分钟无新日志就按卡住处理，停下来查。
6. **验收只认真实产物**（应有值 vs 实有值）；首轮失败是常态，必须补跑并再对账。
7. **需要人工验证（滑块/验证码/短信/扫码）停下来叫人**；任何会真实提交/发送的动作（发票回传、企微发送）先问用户。
8. 1号转接的**真实写操作**（改派客户）：只在明确需要时做，测试性改派必须用户点名会话。
9. **本仓库可能同时有另一个会话在提交**（2026-09-16 17:11 的 `46adbb2` 就把本机未提交的改动一并提交推送了）。**动手前先 `git status` / `git pull`，改完及早提交以免被别人的整提交卷走**；提交信息可能与你做的事无关，别只看 message 判断归属。
10. **截图能力与截图壳已全部删除**（见第二·补、二·补·2、二·补·3）：源码里没有截图能力，也没有「只为截图存在」的菜单/字段/空目录。要看现场就用**文本凭证**（失败原因.txt + 日志 + 平台后台）。若以后真要恢复截图，得从 git 历史里找 `fc4448a` 之前的 `downloadEvidence.js` 那套。**别再给“截图”加开关或半拆。**
