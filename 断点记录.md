# 断点记录（多项目自动化执行任务）

> 覆盖更新，只保留最新一份。位置：`D:\桌面\办公软件\断点记录.md`
> 最近更新：**2026-09-16 09:30** ｜ 状态：**P1~P4 全部完成**（本轮范围：1号 → 2号 → 5号 → 9号）；只有需要你拍板的事项未做

## 一、本次任务（2026-09-16，用户指定顺序）

1. **P1** `1.客服超时督办`：后台启动运行；验收＝客户菜单「新鲜度」≥90%；看客服是否及时回复
2. **P2** `2.发票自动化`：巡检出需要处理的发票，收集汇报
3. **P3** `5.电话漏接分析`：菜单7 下载最新数据 → 菜单1/3 口径查今天+昨天的**排队呼损**，列出需回访号码
4. **P4** `9.客服数据自动更新`：菜单2 全部汇总 → 菜单5 金山一键同步 → 设置透视筛选日期=最新

执行清单：`验收清单.md`。执行铁律与提示词模板：`自动化执行经验.md`、`AGENTS.md`。

## 二、四项结果（全部完成）

| 阶段 | 状态 | 关键事实 / 产物 |
|---|---|---|
| P1 `1.客服超时督办` | ✅ 通过 | 后台启动 `node src/main.js run`，**PID 27496**（08:33:46 启动）。**新鲜度 95%**（阈值 120s）→ 验收通过。客户 **89**：87 正常、2 条历史挂账（德行天下 08-31、Z.🐰 09-04，**会话均已结束**）。**今天新增超时/漏回复提醒 = 0** |
| P2 `2.发票自动化` | ✅ 完成（只读） | 京东开票巡检 5/5 店成功、识别 0 条；京东催票 5/5 店成功、本地 11 条（processing 4 / handled 7，**待处理 0**）；天猫 4 店 + 拼多多 2 店 + 抖音 2 店只读同步 **8/8 成功**。**待处理合计 12 单**＝天猫 10 + 拼多多 1 + 抖音 1，其中 **3 单已超 7 个工作日** |
| P3 `5.电话漏接分析` | ✅ 完成 | 菜单7 下载成功（范围 **2026-06-19~2026-09-16**）。今天+昨天（09-15~09-16）呼损 **12** 条＝**排队 0** + IVR 12 → **需回访 0 个**。报告 `runtime/queue_loss_report_20260916.txt` |
| P4 `9.客服数据自动更新` | ✅ 完成（9/9） | 本地「数据明细」**90 行 / 9 家店**，日期 **2026-09-01~2026-09-14**，合计销售额 **640,893.42**；金山在线 **90 = 本地 90**；透视筛选 **2026-09-14，22/22 成功**。首轮 6/9，**三次补跑补齐** |

## 三、本次新增/修改的代码（已提交推送）

| 提交 | 内容 |
|---|---|
| `42695de` | **2号新增 `scripts/每日只读巡检.js`**：一次跑完京东开票巡检 + 京东催票识别 + 天猫/拼多多/抖音只读同步，输出待处理清单与账龄；每步带墙钟上限；严格只读 |
| （见本次提交） | **5号 `scripts/query_queue_loss.py` 增强**：查询结果自动落盘到 `runtime/queue_loss_report_<日期>.txt`（原来只打屏幕），并修掉 Tee 收尾写已关闭文件的 ValueError |

## 四、本次踩坑与修正（下次直接照做）

1. **Windows 子进程 spawn**：`shell:true` 会去 spawn `cmd.exe`、裸写 `"node"` 不走 PATHEXT，**两者都可能 ENOENT** → 统一用 `process.execPath` + **不加 shell**。
2. **cwd 拼错会伪装成 ENOENT**：`path.join(项目根, "2.发票自动化")` 拼出 `...\2.发票自动化\2.发票自动化`（不存在），spawn 报 `ENOENT`。**看到 ENOENT 先确认 cwd 与命令路径都存在**。
3. **京东登录态隔夜失效**：2号项目 08:41 请用户完成滑块后，5 家店复用登录态一次通过 → **早上先看京东登录**，失败就叫用户，别自己点。
4. **无头页面全白 = 页面没渲染**（京东6店），不是登录问题；重跑即可。
5. **拼多多导出偶发不落盘**：`点击下载表单后 60 秒内没有检测到新文件`。**昨天修复的墙钟上限已生效**（昨天同类问题静默卡死 43 分钟，今天 60 秒报错）→ 直接补跑，通常第 2~3 次成功。
6. `runtime/supervisor.pid` 是**我自己的便签文件**，1号项目代码并不管理它，别当成程序状态。

## 五、可复用命令

```bash
# ---- P1 1号：后台启动 + 新鲜度自检 ----
cd "1.客服超时督办" && node src/main.js run          # 等价 TUI「后台启动」（taskConfig.js:11-20）
cd "1.客服超时督办" && node -e "const{readMissedReplyMonitorState}=require('./src/features/missedReplyMonitor/missedReplyStateStore');const{buildCustomerMirrorItems}=require('./src/controlCenter/customerMirrorListBuilder');const{buildDataFreshness}=require('./src/controlCenter/customerDashboard...')"

# ---- P2 2号：每日只读巡检（三步全跑 / 跳过指定步骤）----
cd "2.发票自动化" && node scripts/每日只读巡检.js
cd "2.发票自动化" && node scripts/每日只读巡检.js --跳过京东开票 --跳过京东催票

# ---- P3 5号：菜单7 下载 + 排队呼损查询（自动落盘报告）----
cd "5.电话漏接分析/电话漏接分析后台" && python scripts/auto_download_and_analyze.py
cd "5.电话漏接分析/电话漏接分析后台" && python scripts/query_queue_loss.py

# ---- P4 9号：全部汇总 + 金山同步 + 透视筛选 ----
cd "9.客服数据自动更新" && node scripts/runSummaryAndKdocsSync.js
cd "9.客服数据自动更新" && node scripts/retryFailedStores.js jd-jd6-all pdd-pdd02-all   # 补跑指定店铺（任务ID：<平台>-<key>-all）
```

## 六、等你拍板

1. **P2 发票 12 单待处理**（天猫 10、拼多多 1、抖音 1），其中 **3 单超 7 个工作日**：
   - 天猫店铺2 `3317002428014087465` ¥1364（09-02 申请，10 个工作日）
   - 天猫店铺2 `5127724189085019444` ¥300（09-03，9 个工作日）
   - 天猫店铺1 `5127726025231004022` ¥458（09-05，8 个工作日）
   - 本次**只读巡检，未做任何平台操作**（不提交、不回传）；要不要推进？（天猫「待同意」单需在天猫后台点同意）
2. **P3 的 12 条 IVR 呼损**要不要一并安排回访？（按你口径「只看排队呼损」未计入）
3. **京东登录态**：今天早上曾失效一次（已请你完成滑块）。若希望不再需要人工，可考虑把 9号/2号 的京东账号目录统一，减少重复登录。
4. 昨天的遗留项仍在等：`自动化执行经验.md` 是否也贴一份到 `D:\SoftTalk\AGENTS.md`。

## 六之二、1号「超时自动转接」升级（2026-09-16，**已上线，口径已定稿并简化**）

诉求（用户口径）：被超时提醒的客服如果**没上班**，自动转给**当前上班**的客服；**售前→售前、售后→售后**；运营不接待，其客户转当班售前。

**最终口径（用户 2026-09-16 定稿）——核心只有一句话“客户消息必须有人回”**：

- 触发：原接待**已过自己班次的时间窗**（早班 08:00~16:30；售前晚班 15:45~23:45；售后晚班 14:00~22:30），运营账号一律视为不在班。
- 目标：**同组**（售前→售前、售后→售后、运营→售前）+ **当天当班**（本人班次时间窗覆盖当前时刻）+ **已上线**（售前看「可被转接」、售后看「自动分配」），按排班表从上到下取第一个满足的。
- **不看值班背景色、不看组长、不看值班标记**——颜色只留 `排班颜色清单` 诊断日志。
- 当班的人都不可接（没上线/没人当班）→ 不转并 **@主管**；非工作时段不转也不发群。
- 跨组一律不转；售后 22:30 下班后、售前 23:45 下班后属于非工作时段，不转也不发群（避免夜里刷屏）。
- 提醒已发出时先看上班监控快照：快照过期/缺该成员则按“无依据”不转，只记日志。

关键发现与改动：

- **原来的 `/api/chat/assign` 根本不存在（HTTP 404）**。平台把“分配会话”做成 **socket.io 事件**：页面只有一条 `wss://<域名>/ws/socket.io/`，上面挂 `/client`（聊天与分配事件）和 `/notify`（只收通知）两个命名空间，事件帧形如 `42/client,["assignChat",...]`。已实测捕获真实帧（2026-09-16 11:00）。
- 新增 `transferMonitor/appSocketFrameProbe.js`（页面脚本前装探针、发 `/client` 事件帧）、`autoTransferVerificationStore.js`（按联系人快照复核，指令发出≠成功）、`autoTransferNotifier.js`（成功发群、失败@主管）、`onlinePresenceMonitor/onlinePresenceSnapshotStore.js`（上班监控每轮把成员开关登记为共享快照）。
- 重写 `timeoutAutoTransfer/timeoutAutoTransferPolicy.js`（原来只支持“运营→值班售前”）；`main.js` 启动时装 socket 探针；配置项 `timeoutAutoTransferEnabled` 文案改为“超时自动转接”。
- 测试：`npm test` **427/427 通过**（原基调 379，新增 48 项）。

**未完成的最后一步（需要真实场景才能验收）**：没有真的转过一个客户。当时只有 2 条会话挂在运营名下，而最新消息是黎经理本人在回复（活人对话），不适合当测试样本；其他会话的原接待都在当班。第一例真实转接会自动复核，成功/失败都会发企微群，失败会@黎路遥。

**转接口已实测打通（2026-09-16 11:39，关键里程碑）**：

用户提议“用我自己的微信号当客户，你转给我”。实际执行：

1. 用户用个人微信（昵称黎路遥）发了一条“测试，免回复”，系统自动分给李守耀；
2. 停后台→用同一登录态跑脚本，发 `42/client,["assignChat",{chatId,groupId,assigneeId}]`，读联系人接口复核：

| 步骤 | 发帧前 assignedTo | 发帧后 assignedTo | 结论 |
|---|---|---|---|
| 转给黎经理席位 | 李守耀 6a3dd793… | **黎经理 693666…067** | ✅ 通道生效 |
| 再转给柯紫婷（售后晚班） | 黎经理 693666…067 | **柯紫婷 698059a5…** | ✅ 再次生效 |

3. 恢复后台，引擎对这条测试会话给出真实裁决：11:39:53 `原接待=柯紫婷（售后，不在班），原因=no_colored_duty_member` → @主管通知已发出（即为设计行为）。

**新增诊断能力**：`排班颜色清单` 日志（每天每个进程一行），直接输出整表带色单元格数与当天带色人员+颜色。

**待用户拍板的口径缺口（售后）→ 已于 2026-09-16 12:00 按用户口径修正并验证**：用户确认“售后组长李守耀在班就是他值班，所以不标颜色”。已实现：值班人 = 当天该班次的组长（无颜色也算）+ 带色客服（售前浅绿 #E2F0D9、**售后浅蓝 #BDD7EE = 售后值班（用户已确认）**），组长优先；无人可接才@主管。首例全链路自动转接已真实生效（12:00:42）。黄色 #FFFF00 = 排班客服的预留假期占位，无值班含义（用户已确认）。

**首例全链路自动转接（2026-09-16 12:00:42，最终验收通过）**：
```
[12:00:42] [超时自动转接][已发送转接指令] 客户=黎路遥，触发=missedReply，原接待=柯紫婷（售后，不在班），目标=李守耀（售后当班，值班来源=组长在班），命名空间=/client,
[12:00:44] [超时自动转接][转接已核验] 客户=黎路遥，原接待=柯紫婷，现接待=李守耀，耗时=2秒
[12:00:44] [超时自动转接][发送转接成功通知] 群=小程序商城&客服对接群
```

接口复核：该会话 `assignedTo` = 6a3dd793a01a1e9da2bf2e6d（李守耀）✅

**背景色口径（用户 2026-09-16 全部确认）**：浅绿 #E2F0D9=售前值班（早/晚各一人）；**浅蓝 #BDD7EE=售后值班（用户已确认）**（两个晩班人只标一个）；**黄色 #FFFF00 及空白格上的颜色=排班客服预留假期占位，无值班含义；休息的人直接忽略**。代码已把黄色列入无意义色并只收早/晚班，休息/年假/行政/空白一律不作候选。

**值班规则已整体简化删除（2026-09-16 18:00，用户定稿）**：用户明确“简单的口径就是售前转售前，售后转售后，运营的接待算售前；无论是售前还是售后，只要有人在线就可以转，只不过硬规则是只转当班上线了的客服，否则转了没人回复”“核心关键是客户的信息有人回复就这么简单”。据此删掉 `listDutyGroupMembers` / `listGroupLeaderDutyMembers` / `isGroupLeaderRoleLabel` / `isDutyMarkedShift` / `collectDutyCandidates` / `listColoredStaffNamesByGroup` / `targetDutySource` 等一整套值班/组长/颜色选人逻辑，改为单一规则：**同组 + 当班 + 在线**（`listOnShiftGroupMembers` + `pickOnlineTarget`）；原因名 `no_duty_member`→`no_on_shift_member`、`duty_member_offline`→`on_shift_member_offline`，`background_color_unavailable` 随颜色规则一起删除（排班表读不到改为 `schedule_unavailable`，静默不打扰）。issue 016 更名为 `016-超时自动转接当班在线客服.md`。

**在班判定缺陷已修（2026-09-16 14:35，提交 5d924ea）**：原逻辑用“本人班次==当前应值班阶段”，但早/晚班有重叠期（售后 14:00~16:30、售前 15:45~16:30），重叠期内早班人还在上班却被判“不在班”→ 客户会被错误转走。已改为按**本人班次时间窗**（早 8:00~16:30、晚 15:45|14:00~23:45|22:30）判定，重叠期早班人视为在班；只有过了自己的下班时间才转。测试 432→436 全绿。

## 七、注意事项（踩坑）

- **端口**：9号 与 19号 都用 **9333**（必须串行）；12号 9334；1号 9371（当前被银行 U 盾服务 `DBWebServer_wss_2GBCM.exe` 占用，与本项目无关）。
- 跑 9号/12号 汇总前确认目标 Excel 未被 WPS/Excel 占用（`fs.openSync(path,'r+')` 试写判断）。
- 9号 补跑单店**不会清空**其他店铺数据；每次补跑脚本末尾会自动再做金山同步 + 透视筛选。
- 9号 douyin2（dedakj抖音）未配置账号密码，登录态失效时只能人工短信登录。
- 1号 后台督办是**常驻服务**，跑完别的项目后记得确认它还活着（`powershell -Command "Get-Process -Id <PID>"`）。
