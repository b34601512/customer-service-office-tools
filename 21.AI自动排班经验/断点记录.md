# 断点记录（下次从这里接着做）

> 位置：`21.AI自动排班经验/断点记录.md`｜更新时间：**2026-09-24**｜仓库 `D:/桌面/办公软件`（main = origin/main）

## 一、刚做完（2026-09-24）

- **10 月售前值班绿标已写在线表**：用户手工调过在线售前班次（与我方 09-20 计划差 9 格）→ 以在线为准，本地计划同步后重算 duty，写 **62 格浅绿**（校验和 b38d11d9）；写后 20 号只读对拍：**底色变化正好 62 格 = 值班名单、班次值 0 变化、人工浅蓝标记全保留**。
- **被迫连值 3 处**（换谁都连，按 warn 口径）：麦诺谦 10/6→7、刘秀文 10/15→16、刘秀文 10/28→29；`验排班表.py` 新增 DP 判“最少必然连值”（反例回归 bad 14 / bad5 8 / month30 全过）。
- **脚本拆分**：写班次 `工具/AirScript-写售前排班.txt`（2026-09-20.1，在线原有未动）；写底色 `工具/AirScript-写值班底色.txt`（2026-09-24.1，141 行，含只读诊断）；配置段 `kdocsDutyColorSync`（webhook+令牌，gitignored）。在线有两个同内容脚本（「写售前值班」和「写底色」），用的是「写底色」那个，另一个可删。
- 教训：脚本文件要**带 BOM 的 UTF-8**；复制给用户走剪贴板（PowerShell Set-Clipboard），别用 `cat | clip`（GBK 乱码）。

## 二、下次要做的

1. **下月（11 月）排班**：① 现读 10 月末在线状态（跨月 trail，主管可能改表）；② 问应休（`工具/推应休天数.py` 推大小周 + 法定节假日问人事）；③ 复制 `工具/解排班示例.py` 改顶部配置；④ `工具/写排班表.py` 本地写 + `工具/验排班表.py` 验收（✗ 清零）；⑤ `工具/写在线排班表.cjs` 写班次 + `工具/写在线值班底色.cjs` 写绿标（**都要用户批准**）+ 20 号只读对拍。
2. 在线表 10 月**售后**被主管手改过（与本地计划差很多），本地 `after` 段没同步、只作样例；要用本地 after 写在线前先问用户。
3. 等用户拍板（未变）：组长在岗要不要显眼标记；「应到」是否逐人算。

## 三、常用命令

```bash
cd "D:/桌面/办公软件/21.AI自动排班经验"
# 1) 现读在线表（写前/写后都要，实时为准）
NODE_PATH="D:/桌面/办公软件/1.客服超时督办/node_modules" node "../20.经验大全/金山在线表格排班表读取/金山在线表格排班表读取.cjs" --url "https://www.kdocs.cn/l/cga7jWGHxzkp" --date 2026-10-15 --sheet "2026年10月" --out ".pi/read"
# 2) 本地写样例 + 验收
PYTHONIOENCODING=utf-8 python "工具/写排班表.py" --xlsx "测试数据/2026年10月客服排班表.xlsx" --plan "测试数据/2026年10月-计划.json" --tsv "测试数据/2026年10月客服排班表.tsv"
PYTHONIOENCODING=utf-8 python "工具/验排班表.py" --xlsx "测试数据/2026年10月客服排班表.xlsx" --prev "测试数据/2026年9月-快照.tsv" --lead 李守耀
# 3) 写在线（**用户批准后**；--dry-run/--diagnose 只读不写）
node "工具/写在线排班表.cjs"   --plan "测试数据/2026年10月-计划.json" --sheet "2026年10月" --dry-run
node "工具/写在线值班底色.cjs" --plan "测试数据/2026年10月-计划.json" --sheet "2026年10月" --dry-run
node "工具/写在线值班底色.cjs" --plan "测试数据/2026年10月-计划.json" --sheet "2026年10月" --diagnose
node "工具/写在线值班底色.cjs" --plan "测试数据/2026年10月-计划.json" --sheet "2026年10月"
```

环境：Python 3.10 + openpyxl；中文输出要 `PYTHONIOENCODING=utf-8`；`*.xlsx` 不入库；在线只读脚本要 `NODE_PATH` 指向 `1.客服超时督办/node_modules`。

## 四、循环状态

- 10 月任务 loop（Loop #1）已完成；下次新任务先拉 loop 托管，干完当场 commit+push。
