# 2026 年 10 月排班表（格式样例）

一份**格式样例**：结构与线上金山排班表一致（含班次与背景色），用于对照表结构、底色约定和读取规则；也可上传金山文档后用 20 号只读脚本对拍。

⚠️ 这不是"生成模板"：表里的排班内容只是示例（售前 2 早 2 晚 1 休）。每月实际怎么排，看 [`../排班经验.md`](../排班经验.md) 和 [`../排班教程经验.md`](../排班教程经验.md)，结合实际判断。

## 文件

| 文件 | 说明 |
| --- | --- |
| `2026年10月客服排班表.xlsx` | 主文件，工作表名 `2026年10月`，含班次与背景色 |
| `2026年10月客服排班表.tsv` | 无颜色纯文本对照（UTF-8 BOM） |

查看方式：直接双击 xlsx；快速文本核对用 TSV；要读成结构/底色摘要：

```powershell
cd "D:\桌面\办公软件\21.AI自动排班经验"
python "工具\读排班表.py" --xlsx "测试数据\2026年10月客服排班表.xlsx"
```

## 布局（与线上表一致）

- 行：`0` 标题 → `1` 日期（含「日期」表头）→ `2` 星期 + 统计表头 → `3-7` 售前 5 人 → `8` 售前小计 → `9` 月份标记（非员工行）→ `10-14` 售后 5 人 → `16` 工作时间 → `17-19` 售前班次汇总 → `21-23` 售后班次汇总 → `25` 上班人数。
- 列：`A` 分组（合并单元格）｜`B` 姓名｜`C..AG` 1..31 日｜`AH` 间隔｜`AI..AP` 剩余、年假、休息、早班、晚班、实到、应到、备注。
- 读取要点（与 20 号经验一致）：姓名列 = 「日期」表头所在列；日期列 = 表头里等于日号的首列；空姓名行与结构行（早班/晚班/休息/上班人数…）必须跳过，不能当员工。

## 颜色图例（模拟线上约定）

| 颜色 | 色值 | 含义 |
| --- | --- | --- |
| 绿 | `#E2F0D9` | 售前值班：每天早班、晚班各 1 人 |
| 黄 | `#FFFF00` | 售后值班组长（李守耀）当班 |
| 蓝 | `#BDD7EE` | 售后当班 |
| 橙 | `#FBE5D6` | 公休/调休标记（非值班） |
| 红 | `#FF0000` | 非员工行的月份标记（“10月”） |
| 白 | `#FFFFFF` | 显式白底，等同无标记 |

底色是值班判定的唯一标记依据；读取时必须用 `getAppliedXf`（有效样式），`getXfByCell` 读不到条件格式色。本文件用静态填充模拟；线上真实表是条件格式色，两者都能被 `getAppliedXf` 读到。

## 班次代码

| 代码 | 含义 |
| --- | --- |
| `早` | 早班 |
| `晚` | 晚班 |
| 空白 | 休息 |
| `年` | 年假（客服自行申请，**本样例不主动排**；排班休息直接记空白） |
| `行` | 行政班 |

## 这份样例表的排法（仅示例）

- 售前 5 人每天 2 早 2 晚 1 休；休息日错开（相邻休隔 3~6 天），不搞固定周期。
- 晚班成段（1~4 天）且**只接休息或继续晚班**，不出现「晚接早」；连续上班 ≤6 天。
- 值班绿标每天早、晚各 1 人；**同一人不连续两天值班**，全月每人 12~13 次。
- 售后组长李守耀「上五休一」全早；其余 4 人错开轮休，**每天保证 2 个早班**，晚班 1~2 个（李守耀休假日 4 人全上，2 早 2 晚）。
- `行` 只落在原本休息的格子，不影响当班覆盖；**年假不主动排**（客服自行申请，休息用剩余假）；李守耀休假日不再安排第二人休。
- 特殊格：柯紫婷 10-13、缪婷婷 10-14 为公休橙；麦诺谦 10-16 为显式白底；月份标记「10月」为红色非员工行。
- 本版为 2026-09-13 重排：上一版按固定 5 天循环取模，出现同一人连续 4 天值班的「机械感」，本版改为按人错开。
- 统计列是示例：**剩余列演示“从上月继承”**（填入 9 月快照结余：韩欢欢 0、麦诺谦 1天、叶炳辉 -3小时、徐佳楠 5天、刘秀文 3天4小时、李守耀 1天、缪婷婷 -1天、柯紫婷 5天、邓远祥 1小时、陈燕玲 1天3小时）；真实口径以 [`../排班教程经验.md`](../排班教程经验.md) 为准（剩余 = 上月剩余 + 应休 − 实休）。

## 用 20 号只读脚本对拍（可选）

```powershell
$env:NODE_PATH = "D:\桌面\办公软件\1.客服超时督办\node_modules"
node "D:\桌面\办公软件\20.经验大全\金山在线表格排班表读取\金山在线表格排班表读取.cjs" --url "分享链接" --date 2026-10-13 --out "输出目录"
```

关注：工作表名 `2026年10月`、矩阵行数、`背景色可读取: true`、10 月 13 日班次与底色（李守耀 早 `#FFFF00`、邓远祥 早 `#BDD7EE`、缪婷婷 晚 `#BDD7EE`、韩欢欢 早 `#E2F0D9`、麦诺谦 晚 `#E2F0D9`、徐佳楠 晚/刘秀文 早 无底色、叶炳辉 休 无底色、柯紫婷 休 `#FBE5D6`、陈燕玲 行 无底色）。

读取原理与常见坑见 [`20.经验大全/金山在线表格排班表读取/金山在线表格排班表读取经验.md`](../../20.经验大全/金山在线表格排班表读取/金山在线表格排班表读取经验.md)。
