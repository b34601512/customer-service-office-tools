# 断点记录（6.抖音发票回传，2026-09-20 13:10 更新）

## 今天做了什么
- 修掉「报表落盘失败」（issues/007）：报表先走 HTTP `/order/invoice/download`（application/download），承载页几秒后自关 → blob/context 一起失效。改成**点击下载前挂 response 监听、抢存同一次下载的响应字节**落盘（saveAs → 响应字节 → blob 直取）。真机：店铺3 同步成功，导出 7893B = 捕获字节数。
- 修掉「静默卡死」（issues/008）：新增 `限时等待`（墙钟上限），关浮层/滑块检测全部包上限。真机同一位置 13 分钟无输出 → 15 秒明确报错。
- 修掉「每次都要重收短信」：报表页自关=最后一个页签 → Edge 整窗退出（cookie 未落盘）→ 下次回登录页。现每上下文留**常驻保活页**（about:blank），收敛页签时不许关它。真机已见「复用已打开账号浏览器」，不再要短信。
- 两轮全流程复跑通过（12:52、13:05：2 家店、失败 0；诺诺无票 → 各跳过 1 单）。

## 还没好（issues/009，开放中）
- 回传阶段偶发 `读取抖音当前店铺 ID 失败：顶部店铺信息识别到 0 个有效店铺 ID`（今日 12:00/12:35/12:41 三次）；截屏证明页面与店铺面板都正常。
- 已布防：失败时自动把头部 DOM/文本/页签数落到 `runtime/douyin-store-identity-fail.json`（成功样本落 `...-ok.json`，每进程各一份）。
- 13:00-13:05 连续两轮未复现，**没有失败样本 → 不猜、不动选择器**。下次复现先看 fail.json 与 ok.json 的差异再改。

## 下次开工第一步
1. 若 `runtime/douyin-store-identity-fail.json` 存在 → 对比 ok.json 的 `头部.文本 / mask节点数 / label列表`，定根因后修（要求：storeId+storeName 双校验不许放宽、不许模糊兜底），补反向断言测试。
2. 否则直接跑一轮：`printf '6\na\n' | node src/cli/startCli.js`。

## 常用命令 / 环境
- 跑：`printf '6\na\n' | node src/cli/startCli.js`（两店）；单店：`printf '6\n2\n' | node src/cli/startCli.js`。
- 测试：`npm test`（当前 124/124）。
- 日志：`runtime/latest-run.log`；落盘失败台账：`runtime/douyin-download-failures.log`。
- **禁止强杀浏览器进程**（会丢抖音登录态→又要短信）；要关窗用优雅关闭（等同一路）：`CloseMainWindow` 或让用户点 ×。
- 正常跑完浏览器会**留着供人工核实**，再跑前必须先把窗口关掉，否则直接报「检测到残留浏览器」。
- 两店共用一份登录（同手机号 account-270b8179d0d7），**换店只切店、不重登**。
