# 断点记录（6.抖音发票回传，2026-09-21 11:00 更新）

## 本轮结果
- 店铺5已完成人工登录、切店（162329841 → 29502951），同步成功 1 单：`6955060045796152564`，¥1167，平台仍为「待开票」。
- 回传阶段停在「正在打开抖音待开票列表」；10:51:45 起持续有 5 秒进度日志，CLI 观察到 315 秒仍未就绪，已 Ctrl+C 停止。
- 未进入导出、下载中心、上传或提交；本地记录仍 `workflowStatus=processing`、`handledAt` 为空。页面目标地址为 `https://fxg.jinritemai.com/ffa/morder/receipt/list`。

## 下次开工第一步
1. 先读根目录 `失败台账.md` 的本轮现场；不要直接补跑，先定位列表加载为何不满足就绪条件。
2. 定位完成且确认环境恢复后，才手工执行：`printf '6\n2\n' | node src/cli/startCli.js`。

## 常用命令 / 环境
- 测试：`npm test`（上次 124/124）。
- 日志：`runtime/latest-run.log`；登录/身份现场：`runtime/douyin-store-identity-ok.json`、`runtime/douyin-store-identity-fail.json`。
- 两店共用登录资料 `runtime/account-profiles/msedge/account-270b8179d0d7`；关窗只用 `CloseMainWindow` 或用户点 ×，不要强杀。
