# Issue 003：逐店同步并回传

## 意图与背景

同手机号共享浏览器后，一键操作仍先同步全部店再回传全部店，日志出现 A→B→A。改为 A 同步并回传完成后才进入 B。CLI/TUI 共用 app 层流程，不改其他平台。

## 验收

- [x] 单次任务逐店完整执行，失败继续下一店；不循环重跑。
- [x] 同步失败阻止该店回传；仅消费本次快照中仍待回传的订单。
- [x] 清理抖音入口旧两轮编排与 TUI 假工作台上下文。
- [x] 减少同店重复页面导航，保留身份核验。
- [x] 定向测试顺序、失败隔离、空店、已成功订单与两入口一致性。

## 验证

首轮 npm test：101 项中 100 项通过，历史测试暴露 Issue 005 的真实状态缺陷。修复后 18 项相关复测全部通过，CLI/TUI 模块加载和 diff 检查通过。验证导致生产代码返工一次（补充下载开始时进入 processing）。没有执行真实回传。旧两轮流程已从两个入口移除，公共平台模块保持原样。

## 边界

不实际上传/提交，不改其他项目；修改前源码、测试、文档及 package 已备份到 D:\备份文件夹\抖音逐店处理-20260907。既有未提交改动保留。相关数据缺陷见 Issue 004。
