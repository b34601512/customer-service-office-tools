const 批量单店最大尝试次数 = 2;
// 批量识别完成后保留已打开的店铺页面，便于人工复核，同时继续处理下一家店铺。
const 批量店铺页面保留模式 = 'keep';
// 批量回传完成后保留已打开的京东回传页面，便于人工核对；由程序退出时统一关闭。
const 批量回传页面保留模式 = 'keep';

module.exports = {
  批量单店最大尝试次数,
  批量店铺页面保留模式,
  批量回传页面保留模式,
};
