function 计算新增记录(旧记录列表, 新记录列表) {
  // 解决：只提醒新出现的记录，避免每次巡检都重复打扰。
  const 历史标识集合 = new Set((旧记录列表 ?? []).map((记录) => 记录.id));
  return (新记录列表 ?? []).filter((记录) => !历史标识集合.has(记录.id));
}

module.exports = {
  计算新增记录,
};
