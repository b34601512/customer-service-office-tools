# 汇总中途重新读取了实时配置

意图：一次汇总使用开始时的同一份配置。
背景：summaryTaskRuntime 接受 projectConfig，却调用内部重新读盘的 resolvePlatformStoreConfig；期间修改店铺/开关可能使任务清单、账号和来源不一致。
方案：解析器允许显式传入配置，本轮任务传递既有快照，其他入口保持原行为。

- [x] 配置快照贯穿店铺报表解析。
- [x] 验证与磁盘配置不同的传入配置仍生效。

2026-09-05：解析只存在于传入快照的 snapshot-only 店铺成功，修改返回运行态也不污染快照。原入口未传快照时仍调用原读取服务。
