# 16号项目 v10 免安装包

## 意图与背景

用户要求升级版本号、重新打包给朋友。沿用原单文件 PyInstaller 免安装 EXE，纳入 issue 005/006 的中文输入修复及配置验收。

## 边界

新建 v10 构建和发布目录，保留旧发布包；仅包含 EXE、说明、公开示例清单和原示例结果，不打入登录资料、日志及个人采集数据。

## 验收

- [x] 界面、README、使用说明统一为 v10。
- [x] 生成免 Python EXE，确认 console_input 随包内置。
- [x] 成品启动及配置验证。
- [x] ZIP完整性及内容核验后交付路径。

## 结果

PyInstaller 6.7.0 单文件 console 模式构建成功。读取成品嵌入归档，确认 console_input、boss_cdp、merchant_subjects、shop_subjects、requests、websocket 均存在。
独立隐藏 Windows 控制台运行实际 EXE（不是源码替身），读取真实屏幕确认 v10；注入城市“济南”、岗位“客服,仓管”，验证保存、取消、q返回首页、重新进入配置仍保留数据，Ctrl+C退出码0。
验证脚本保存在项目 build/16号项目-v10/verify_exe.py，仅用于构建验收，不进入发布包。
成品 ZIP：发布/BOSS直聘获取公司名称-免安装版-20260905-v10.zip，约10.47 MiB。
ZIP共4文件：EXE、使用说明、公开店铺示例清单、原3家示例CSV；CRC及逐文件与发布目录字节比对通过。
本次成品验证未导致生产代码返工。旧发布包保留。
