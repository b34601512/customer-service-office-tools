# 浏览器启动失败可能直接退出客户端

意图：浏览器被系统拒绝启动时显示失败原因，客户端仍可继续操作。
背景：spawn 的异步 error 无监听，启动流程已提前记录 PID；外层 async catch 捕获不到 EventEmitter 的未处理 error。
方案：等待 spawn/error 后才登记会话；不运行真实浏览器做负向回归。

- [x] 启动成功后才记录 PID 和会话。
- [x] 模拟异步启动错误可被调用方捕获。

2026-09-05：分别模拟 spawn 成功和异步 error，返回/拒绝均正确；未启动真实浏览器。
