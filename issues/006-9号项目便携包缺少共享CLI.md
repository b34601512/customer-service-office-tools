# 9号项目便携包启动 MODULE_NOT_FOUND

## 意图与背景
其他客服主管解压客户端后可直接启动，不依赖开发电脑目录。截图指向 startTuiRuntime.js:16；该行引用项目同级的共享CLI/最大化控制台窗口.js，旧打包脚本只收集项目内 src 和 node_modules，漏掉共享模块。原校验只查压缩包文件列表，没有加载 TUI。

## 方案与边界
包内以“应用”和“共享CLI”保留源码相对位置；根目录保留双击启动入口。共享逻辑仍只维护仓库原文件，不改业务、不增加缺失模块兜底。校验须在仓库外解压并用内置 Node 加载和渲染 TUI。不得带入账号、登录态或业务数据；保留已有修改和历史包。

## 验收
- [x] 打包包含共享模块，源代码相对引用保持有效。
- [x] 新包在仓库外完成首次配置初始化、TUI 页面渲染。
- [x] 缺少共享模块的旧包被同一校验拒绝。
- [x] 提供可直接转发的新版本压缩包。

## 验证记录
2026-09-05：v1.0.3 打包成功，610 个文件、39.16 MB。包内 Node 在 D:/备份文件夹/客服数据自动更新/打包校验/7d6d91d663d24bcca6bb7eff9adc51cd 下完成首次配置初始化、六页渲染及四个运行依赖加载；确认加载模块均在包内。旧 v1.0.2 ZIP 另行独立解压，原样复现 startTuiRuntime.js:16:24 缺少 ../../../../共享CLI/最大化控制台窗口，校验退出码 1。

新包 PowerShell 启动入口通过管道输入 0，正常显示菜单并退出。当前执行环境无法创建交互式 PTY，因此 TUI 使用真实初始化和页面渲染、模拟输出终端验证，未实测鼠标双击可见窗口；未登录客户平台或执行汇总。git diff --check 通过。验证未导致生产代码返工。

交付：9.客服数据自动更新/发布包/客服数据自动更新-v1.0.3.zip。原 v1.0.2 ZIP 和已解压目录保留。源码已有版本修改从 1.0.2 正常更新到 1.0.3，其余未提交工作未覆盖。
