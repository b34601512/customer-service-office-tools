# 16号项目交付前配置验收

## 意图

交付朋友前核验五项配置的编辑、保存、取消、返回以及采集参数传递，避免只修复输入而遗漏可用性。

## 背景与边界

延续本地 issue 005 的 v9.1 输入修复。完整复核配置页、Ctx.start_fetch、run_fetch、fetch_page、export_rows 和启动脚本。
本次以配置验收为主，不把离线测试说成真实网站抓取成功，不操作用户登录浏览器。
现状：城市中文名内置24个（含济南、杭州），其余需要城市码；配置只在当前进程保存，重启恢复默认。

## 验收清单

- [x] 完整阅读配置至业务的相关链路。
- [x] 真实控制台事件逐项验证五字段提交、取消、无效输入及快捷键隔离。
- [x] 核验三种导出格式、页数上下限、岗位条件留空和重置。
- [x] 检查首页采集向业务传递全部已配置参数。
- [x] 批量自检并明确实际输入法和联网验证边界。

## 验证结果（2026-09-05）

扩展现有真实控制台测试：搜索词“电商客服qr123”、城市“济南”、页数400、岗位“客服，仓管”均保存成功，逐字段取消不改变原值；中文中的q/r/数字不触发全局快捷键。
空搜索词、空城市、空页数及页数0/1001拒绝且保留原配置；1/1000边界方向键不越界；岗位可清空；csv/json/both切换和取消正常；r重置、q返回正常。
首页任务调用核对：搜索词、城市、页数、格式、岗位过滤条件均正确传入run_fetch。
`python -B selftest.py`：51通过、0失败、3跳过（真实浏览器与网站抓取）；diff检查通过。
本轮批量验证1次，生产代码返工0次，仅扩展验收测试。

## 交付提醒

应发送完整v9.1项目，不能只发送boss_tui.py而漏掉console_input.py。
页数表示采集上限，不保证网站提供对应数量；中文城市只支持CITY_CODES列出的24个，其他城市可填城市码。
回车保存是本次运行内的配置，关闭重启恢复默认；未新增持久化功能。

## 不可误报

真实 Win32 Unicode 事件测试覆盖已提交的中文字符，不代表已操作朋友输入法的候选框。
未真实登录和联网采集，不能承诺网站可用性。
