# 原始表加载被 Excel COM 锁死

## 意图

消除原始表和客服统计加载时的 Excel COM 卡死；一次下载只解析一次，页面统一复用同一份结果数据。

## 背景

2026-08-31 下载分析已成功读取三表；随后 `refresh_result` 再次用 Excel COM 预读。日志停在呼入表“读取完成”之后，原表锁未释放，多个页面加载线程无限等待。相同呼出文件隔离读取 236 行仅需 1.27 秒，排除文件损坏和数据量问题。现有三份实表经 COM 与 xlrd 全量比对，行数及非空单元格值一致。

## 验收清单

- [x] 运行链路不再启动 Excel COM。
- [x] 每轮下载的三张表只解析一次。
- [x] 原始表与客服统计复用结果中的同一份原始行。
- [x] 当前旧结果可一次升级到新结构，升级后不再读取 Excel。
- [x] 呼损、呼入、呼出数量保持 1072、2057、236。
- [x] 自动测试与真实数据启动检查通过。

## 边界

- 不增加超时兜底、重试绕行或第二套运行时真源。
- 不改变呼损、呼入、呼出及客服统计业务口径。
- 原始 `.xls` 仅作为下载证据归档，页面不再直接读取。
- 不触碰仓库内其他项目和其他 AI 的改动。

## 验证记录

- 31 项自动测试通过。
- 当天真实结果已升级到 schema 9，三表原始行写入同一 `result.json`。
- 冷读取、呼损页渲染和客服记录构建合计 0.641 秒。
- 验证前后均无 `EXCEL.EXE`，呼损页未出现“正在加载”。
